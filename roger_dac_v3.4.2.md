# Roger DAC v3.4.2

> v3.0的原则，2并联 + 独立+9V I/V供电，系统接口匹配

---

## 0. 从v3.0到v3.4.2

### 0.1 继承什么

v3.0从三条原则推导出了架构：

1. **时序纯净**：独立晶振自由振荡 → 纯分频 → 无PLL
2. **信息直通**：NOS，无数字滤波，无升采样，无DSP
3. **只做必要的事**：每个元件必须回答"为什么需要你"

v3.0的架构决策中，以下来自原则而非器件，v3.4.2全部继承：

| 决策 | 来源 | v3.4.2状态 |
|------|------|----------|
| XMOS单芯片USB+FIFO+重时钟 | 信号路径最短 | 继承 |
| 独立晶振外部MCLK | 时序纯净 | 继承 |
| NOS无数字滤波 | 信息直通 | 继承 |
| 无CPLD/格式适配 | 只做必要的事 | 继承 |
| 无负电源 | 只做必要的事 | 继承 |
| 电池供电 | 物理隔离市电噪声 | 继承 |

### 0.2 改变什么

v3.0的离散并行R2R是原则的终极形态，但工程复杂度过高（50颗精密电阻、32颗开关IC、116颗去耦、走线匹配、热对称）。

v3.4.2用TDA1543替代离散R2R，2并联/声道增强动态，OPA1656独立+9V供电达到标准线电平：

| 维度 | v3.0 离散R2R | v3.4.2 TDA1543 ×4（2并联/声道）|
|------|-------------|-------------------------------|
| 信号路径 | XMOS → FPGA → 开关 → R2R → 缓冲 → LPF | XMOS → TDA1543×2并联 → I/V(+9V) → 四阶Bessel LPF |
| 环节数 | 5 | 4 |
| 格式适配 | FPGA做串→并 | 不需要（原生I2S） |
| 负电源 | 不需要 | 不需要 |
| I/V转换 | 不需要（电压直出） | 需要（电流输出，2颗电流汇合，独立+9V供电） |
| FPGA | 需要（iCE40） | 不需要 |
| 有效位宽 | 14~16 bit | 16 bit（并联等效SNR +3dB，DNL ε/√2） |
| 主要IC数量 | XMOS + FPGA + 32×开关 = 34 | XMOS + 4×TDA1543 = 5 |
| LDO数量 | 3 | 4（新增+9V_IV独立供电，DAC供电+7V改LT1763） |
| 去耦电容 | ~116颗 | ~54颗 |
| 输出电平 | — | ~1.94Vrms（接近2Vrms标准线电平） |
| PCB复杂度 | 高 | 低~中 |

v3.4.2比v3.0**多了**I/V转换（一个有源环节），**少了**FPGA和32颗开关IC。
净效果：信号路径更短（4 vs 5），元件总数大幅减少。
并联提供动态余量（SNR +3dB，DNL ε/√2），独立+9V供电打开输出摆幅天花板——
输出从v3.3的0.78Vrms提升到1.94Vrms，可直接驱动2Vrms灵敏度的耳放/功放。

### 0.3 放弃了什么

| 放弃 | 含义 | 态度 |
|------|------|------|
| 并行更新（一个沿所有bit同时切换） | TDA1543内部仍是串行移位 | 接受——移位在IC内部，不在PCB上 |
| 电压直出（无I/V） | 需要跨阻运放 | 接受——成熟电路，风险低 |
| 可观测/可更换的R2R网络 | IC黑盒 | 接受——用测量验证 |
| 永续供货 | TDA1543已停产 | 风险——需可靠渠道+备货 |

---

## 1. 关键决策表

| 决策 | 选择 | 理由 |
|------|------|------|
| USB接收 | XMOS XU216 + 开源固件 | macOS integer mode，可控 |
| 时钟 | 双独立晶振 → OE选择 → XMOS外部MCLK | 自由振荡，无PLL |
| 晶振切换 | CCHD-957 OE引脚，XMOS GPIO控制 | 无外部MUX芯片，最简硬件（Gate-3验证） |
| I2S帧格式 | 32fs（16bit/声道，无填充BCK） | 语义一致：帧内每个BCK都承载有效数据（Gate-1验证） |
| FIFO | XMOS内部异步FIFO | 单芯片跨时钟域 |
| I2S传输 | XMOS直连TDA1543 | TDA1543原生I2S，无需适配 |
| DAC | TDA1543 × 4（2并联/声道）双单声道 | 原生I2S + 单+7V + NOS + 16-bit + 并联增强动态 |
| I/V | OPA1656 + Rf（**独立+9V供电**） | +9V单电源rail-to-rail，独立LT3042 #3 |
| I/V偏置 | 4.5V（+9V对称中点），PCB支持4.0V/4.5V可调 | TDA1543合规裕量1.3V+OPA1656完美对称，Gate-2验证THD |
| Rf策略 | 2.5kΩ（默认），PCB支持2.0kΩ/2.2kΩ/2.5kΩ | 2并联+9V供电→接近标准线电平2Vrms |
| LPF | 四阶无源Bessel fc≈70kHz | 时域优先（同阶最平坦群延迟/最小振铃），镜像@44.1k≥24dB（验收标准见5.7节） |
| 输出缓冲 | 无（I/V运放直驱） | I/V运放输出阻抗已足够低 |
| 电源 | 3S LiFePO4 + 全正电源 | 无负电源，无电荷泵（Gate-4验证温升） |
| 输出耦合 | AC（10μF PP薄膜，DA≤0.05%） | 去除I/V直流偏置，无介质吸收鬼影 |

---

## 2. 系统架构

### 2.1 信号路径

```
USB ──▶ XMOS XU216 ──▶ TDA1543 ×2并联 ──▶ I/V (OPA1656) ──▶ LPF ──▶ RCA
        (FIFO+重时钟)   (I2S→R2R→电流     (电流→电压        (四阶    (AC耦合
         USB→本地时钟)    ×2汇合, +5V)      独立+9V供电)      Bessel)   +阻尼)
```

四个环节。全部版本中最短。并联不增加环节——两颗TDA1543的电流在I/V虚地节点物理叠加。

对比：
- v2.1 (AD1865)：USB → XMOS → CPLD → AD1865 → I/V → LPF → 输出 = 6环节
- v3.0 (离散R2R)：USB → XMOS → FPGA → 开关+R2R → 缓冲 → LPF → 输出 = 5环节
- **v3.4.2 (TDA1543×4)：USB → XMOS → TDA1543×2并联 → I/V(+9V) → 四阶Bessel → 输出 = 4环节**

### 2.2 时钟路径

```
                    OE ← XMOS GPIO
                     │
22.5792 MHz ──22Ω──┤──▶ XMOS MCLK输入 ──▶ 分频 ──▶ BCK/WS ──▶ TDA1543
                     │
24.576 MHz ──22Ω──┤
                     │
                    OE ← XMOS GPIO (互补)
```

**晶振切换机制：OE（Output Enable）控制。**

CCHD-957有OE引脚。OE=HIGH时输出有效，OE=LOW时输出三态（高阻）。
两颗晶振的OE分别连接到XMOS的两个GPIO，固件控制互斥：
- 播放44.1k系（44.1/88.2/176.4kHz）：使能22.5792MHz，禁用24.576MHz
- 播放48k系（48/96/192kHz）：使能24.576MHz，禁用22.5792MHz

**为什么用OE而不用外部MUX芯片：**
- 不增加任何IC（CCHD-957自带OE）
- 不在时钟信号路径上插入MUX的Ron和寄生电容
- 被禁用的晶振输出三态，不向MCLK走线注入噪声
- 符合"只做必要的事"——XMOS已有GPIO，晶振已有OE，不需要第三个器件

**切换时序（固件控制）：**
1. 静音I2S输出
2. 禁用当前晶振OE（→三态）
3. 等待≥1μs（确保前一颗完全释放，避免双驱动）
4. 使能目标晶振OE
5. 等待晶振输出稳定（~2ms）
6. XMOS重新锁定MCLK，建立新BCK/WS
7. 恢复I2S输出

**上电安全：**
- 两颗晶振OE各有10kΩ下拉 → 上电默认关闭（三态）
- **XMOS GPIO必须在固件初始化完成前保持输入模式或低电平**
  （避免上电瞬间GPIO状态不确定导致两颗同时使能或毛刺）
- 固件启动序列：GPIO配置为输出低电平 → 检测采样率 → 使能对应晶振

**切换瞬间的风险与防护：**
- 风险1：OE释放到三态和另一颗OE拉高之间，如果GPIO时序不干净，
  可能出现极短暂的双驱动（两颗同时输出到MCLK走线）
- 风险2：三态悬空期间MCLK线上可能被相邻走线耦合出毛刺
- 防护：步骤2和步骤4之间的≥1μs间隔由固件保证（GPIO操作之间插入delay）
- 10kΩ下拉在三态期间将MCLK线拉到确定电平，减少悬浮耦合
- **验证项（阶段一）**：切换采样率时，示波器同时看MCLK和BCK/WS，
  确认无异常毛刺导致单次错帧（Gate-3，见第16节）

无PLL，无时钟恢复，无门控。XMOS到TDA1543之间没有任何时钟域转换。

### 2.3 电源路径

```
3S LiFePO4 (7.5~10.95V)
    │
    ├──▶ LT3042 #1 ──▶ 3.3V_CLK ──▶ 晶振×2
    │
    ├──▶ LT1763 #2 ──▶ +7V_DAC ──▶ TDA1543 VDD ×4
    │
    ├──▶ LT3042 #3 ──▶ +9V_IV ──▶ OPA1656 V+ ×2
    │                            ──▶ I/V偏置分压 ×2
    │                            ──▶ dummy偏置分压 ×4
    │
    └──▶ LT1763 ──▶ 3.3V_D ──▶ XMOS
```

四路LDO。TDA1543和OPA1656各自独立供电——
TDA1543在+5V（它只需要+5V），OPA1656在+9V（打开输出摆幅天花板）。
独立供电的附加收益：DAC的R2R开关噪声不调制I/V级，电源串扰路径被切断。
无负电源，无电荷泵。

---

## 3. TDA1543

### 3.1 为什么选TDA1543

逐条对照原则：

| 原则 | TDA1543表现 | 对比AD1865 |
|------|-------------|-----------|
| 无需格式适配 | 原生I2S（BCK/DATA/WS） | 5-wire，需CPLD |
| 无需负电源 | 单+5V | 需±5V |
| NOS | 无内部数字滤波器 | 同 |
| 位宽 | 16-bit | 18-bit标称/~16实测 |
| 接口复杂度 | 3线（标准I2S） | 5线（非标准） |

TDA1543是"从原则出发能找到的最简单的R2R DAC IC"。

### 3.2 器件信息

**TDA1543 / TDA1543A**（Philips / NXP）

| 参数 | 值 |
|------|-----|
| 架构 | R2R电流输出 |
| 位宽 | 16-bit |
| 数字接口 | I2S（BCK、DATA、WS） |
| 供电 | 单+7V（VDD） |
| 满幅输出电流 | 典型 ±0.55 mA（以实测为准） |
| 数字滤波 | 无（NOS） |
| 过采样 | 无 |
| 封装 | DIP-8 / SO-8 |
| 内部结构 | 双通道（L+R），共享I2S接口 |

### 3.3 引脚配置

```
TDA1543 DIP-8：

        ┌───╥───┐
  BCK  ─┤1     8├─ VDD (+7V)
  WS   ─┤2     7├─ IOUTL (左声道电流输出)
  DATA ─┤3     6├─ IOUTR (右声道电流输出)
  GND  ─┤4     5├─ GND
        └───────┘
```

I2S格式：
- BCK：位时钟，**下降沿**锁存DATA（标准I2S）
- WS：字选择，LOW=左声道，HIGH=右声道
- DATA：串行数据，MSB first
- 标准I2S格式：WS边沿后1个BCK延迟开始MSB

### 3.4 2并联双单声道配置

```
                        XMOS I2S输出
                        │  │  │
                       BCK WS DATA （3线广播到全部4颗）
                        │  │  │
         ┌──────────────┼──┼──┼──────────────┐
         │              │  │  │              │
    ┌────┴────┐    ┌────┴──┴──┴────┐    ┌────┴────┐
    │ TDA1543 │    │               │    │ TDA1543 │
    │  #1 (L) │    │               │    │  #3 (R) │
    │ IOUTL ──┼─┐  │               │  ┌─┼── IOUTR │
    │ IOUTR   │ │  │               │  │ │  IOUTL  │
    └─────────┘ │  │               │  │ └─────────┘
         │      │  │               │  │      │
    ┌────┴────┐ │  │               │  │ ┌────┴────┐
    │ TDA1543 │ │  │               │  │ │ TDA1543 │
    │  #2 (L) │ │  │               │  │ │  #4 (R) │
    │ IOUTL ──┼─┤  │               │  ├─┼── IOUTR │
    │ IOUTR   │ │  │               │  │ │  IOUTL  │
    └─────────┘ │  │               │  │ └─────────┘
                │  │               │  │
                ▼  │               │  ▼
         L声道Iout汇合            R声道Iout汇合
         (±1.1mA)                (±1.1mA)
                │                     │
                ▼                     ▼
         OPA1656 I/V (L)        OPA1656 I/V (R)
```

**拓扑：4颗TDA1543，分为左右各2颗并联组。**

每组的2颗TDA1543：
- 接收完全相同的I2S信号（3线广播）
- 在同一BCK边沿锁存，Iout同步翻转
- 对应声道的Iout引脚直接并联，汇入同一个I/V运放的虚地节点
- 电流源天然可并联——这是TDA1543的结构性优势，不需要任何额外电路

**并联收益的物理基础：**

```
信号电流：线性叠加 → Iout_total = 2 × Iout_single = ±1.1mA
不相关噪声：√N叠加 → noise_total = √2 × noise_single
SNR提升：20log₁₀(2/√2) = 10log₁₀(2) ≈ +3.0dB

DNL误差平均：
  每颗TDA1543的R2R ladder电阻匹配误差独立
  2颗并联 → DNL误差 ≈ ε/√2（约30%改善）
  小信号（-60dBFS以下）线性度和idle tone显著改善
  这对"框架感"有直接影响——安静段乐器轮廓更清晰

实际效果：
  SNR：~87dB → ~90dB
  输出电压（+9V供电，Rf=2.5kΩ）：~1.94Vrms（比单颗0.78Vrms +7.9dB）
  （其中并联贡献+3.5dB电流翻倍，+9V供电贡献+4.4dB headroom释放）
```

**为什么是2并联而不是4并联：**

| 维度 | 2并联/声道 | 4并联/声道 |
|------|-----------|-----------|
| 输出电压（+9V供电，compliance锁定） | 1.94Vrms（Rf=2.5kΩ） | 1.94Vrms（Rf=1.25kΩ）——相同 |
| SNR | +3dB → ~90dB | +6dB → ~93dB |
| DNL改善 | ε/√2（30%） | ε/2（50%，边际+15%） |
| 总芯片数 | 4颗 | 8颗 |
| PCB面积 | 90×60mm（不变） | ≥100×80mm（必须扩板） |
| BOM增量 | +$15 | +$45 |

关键发现：输出电压被TDA1543 Iout compliance voltage锁死——
不管并联多少颗，偏置点不能超过VDD−1.2V（Datasheet合规上限）。
VDD=7V时，合规上限5.8V，偏置4.5V → 裕量1.3V，设计层面保证安全。
2并联和4并联在相同偏置下产生相同的最大输出电压。
4并联在SNR上多+3dB（90→93），但90dB基准上的边际听感差异很小。
而PCB扩板+停产芯片翻倍消耗带来的代价不可忽视。

芯片分配：
- #1：取IOUTL → 左声道并联组
- #2：取IOUTL → 左声道并联组
- #3：取IOUTR → 右声道并联组
- #4：取IOUTR → 右声道并联组

未用输出（每颗各一路）通过Rf_dummy接到独立偏置点，保持内部电流镜对称。

### 3.5 I2S直连——为什么不需要任何适配

```
XMOS I2S标准模式输出：
  BCK：连续位时钟
  WS：字选择（=LRCK），LOW=左，HIGH=右
  DATA：MSB first，WS边沿后1 BCK delay

TDA1543 I2S输入：
  BCK：位时钟，下降沿锁存
  WS：字选择，LOW=左，HIGH=右
  DATA：MSB first，WS边沿后1 BCK delay
```

格式完全匹配。XMOS的BCK/WS/DATA直接连到TDA1543的BCK/WS/DATA。
不需要CPLD，不需要FPGA，不需要任何胶水逻辑。

三根线。这是v1.0以来所有版本中数字接口最简单的方案。

### 3.6 帧格式与位宽

**关键决策：32fs帧格式（非64fs）。**

TDA1543是16-bit DAC。每声道恰好需要16个BCK周期接收数据。
XMOS配置为32fs输出——每个WS周期包含32个BCK（左16 + 右16），
每个BCK都承载有效数据位，帧内没有空闲时钟。

```c
#define STREAM_FORMAT_OUTPUT_RESOLUTION  16
#define I2S_CHANS_PER_FRAME              2
// 帧格式：32fs = 16bit/声道 × 2声道
// BCK = fs × 32
```

**为什么不用64fs：**

选择32fs不是因为"TDA1543可能对多余BCK敏感"，
而是因为**不把系统正确性建立在器件对多余时钟的容忍性上**。

64fs意味着每声道32个BCK周期，其中只有前16个承载有效数据，
后16个BCK是填充（DATA为0）。工程问题：

1. TDA1543在收到16bit后对剩余BCK脉冲的行为，datasheet未明确定义。
   64fs在实践中可能完全正常——但"正常工作"和"行为被定义"是两件事。
   工程文档不应建立在未定义行为上，即使概率上很可能没问题。
2. 64fs下BCK频率翻倍，在高采样率时更早触及TDA1543的BCK上限（9.2MHz）。

32fs消除了这两个问题：
- 每个BCK都有语义——没有"可能被忽略也可能不被忽略"的填充时钟
- BCK频率减半——为高采样率留出结构性余量

**验证项（阶段一/三）：32fs vs 64fs对照实验**
如果XMOS固件同时支持32fs和64fs，或者阶段一时被迫回退到64fs，
执行以下对照：
- 同一1kHz -60dBFS信号，分别在32fs和64fs下播放
- 比较：线性度、噪声地板、idle tone（闲置音/杂散）
- 很多老DAC的"未定义行为"不表现为功能错误，而表现为杂散或闲置音
- 记录数据，无论结论如何

Roger Player端：
- 检测设备为16-bit → symphonia解码器输出16-bit i32左对齐
- 16-bit源（CD）：bit-perfect
- 24-bit源：截断发生在Roger Player软件层（可控、可审计）

**阶段一验证项**：示波器确认32fs帧格式下BCK/WS/DATA波形——
每个WS半周期恰好包含16个BCK，DATA在每个BCK上都有有效电平。

### 3.7 采样率与时钟

TDA1543 BCK最大频率：**9.2 MHz**（datasheet规格）。
WS最大频率：**192 kHz**（datasheet规格）。

32fs帧格式下的BCK频率：

| 采样率 | 晶振 | BCK (32fs) | BCK/BCKmax | 状态 |
|--------|------|-----------|------------|------|
| 44.1k | 22.5792M | 1.4112M | 15% | 默认支持 |
| 48k | 24.576M | 1.536M | 17% | 默认支持 |
| 88.2k | 22.5792M | 2.8224M | 31% | 默认支持 |
| 96k | 24.576M | 3.072M | 33% | 默认支持 |
| 176.4k | 22.5792M | 5.6448M | 61% | **结构可支持** |
| 192k | 24.576M | 6.144M | 67% | **结构可支持** |

32fs让所有采样率（含176.4k/192k）都在BCK上限的67%以内——充足余量。

**176.4k/192k支持策略：验证后启用。**
结构上不再有BCK频率瓶颈，但需验证：
- TDA1543在高BCK频率下的实际THD/噪声表现
- XMOS固件在32fs + 176.4k/192k配置下的稳定性
- I/V运放在高采样率下的建立时间裕量

对比64fs方案：

| 采样率 | BCK@64fs | BCK@32fs | 差异 |
|--------|----------|----------|------|
| 96k | 6.144M (67%) | 3.072M (33%) | 余量翻倍 |
| 192k | 12.288M (**超限**) | 6.144M (67%) | 从不可能到可行 |

### 3.8 供货与验证

TDA1543已停产。现实策略：

```
采购：
□ 从可信渠道（如Rochester Electronics、正规拆机商）采购
□ 一次采购足够数量（≥12颗，含备用和配对筛选损耗）
□ 优先选择DIP-8封装（易于验证和更换）
□ 并联方案每台需4颗，配对筛选可能淘汰~30%，按每台需6颗采购余量

来料验证：
□ 外观检查：印字、封装一致性
□ 静态测试：VDD=7V，测量静态电流（Datasheet @5V: 50mA typ，7V下预计~52mA）
□ 功能测试：发送1kHz满幅正弦I2S → 示波器看电流输出波形
□ 配对测试：同批多颗芯片在相同信号下输出一致性

如果无法获得可靠的TDA1543：
  备选1：TDA1543A（同系列，略有差异，同样停产但存量更大）
  备选2：回退到v2.1 AD1865方案（仍在产，但需CPLD+负电源）
  备选3：推进v3.0离散R2R（永续供货）
```

---

## 4. XMOS XU216

角色和配置与v3.0相同，仅USB输出分辨率改为16-bit。

### 4.1 职责

| 做什么 | 不做什么 |
|--------|---------|
| USB Audio Class 2.0异步接收 | 不做D/A转换 |
| 内部异步FIFO（跨时钟域） | 不做信号处理 |
| 速率反馈端点（不丢弃样本） | 不做升采样 |
| 外部MCLK输入 + 分频 | 不生成时钟 |
| I2S输出（BCK/WS/DATA） | 不做格式适配 |

### 4.2 时钟架构

```
XMOS GPIO_A ──▶ CCHD-957 #1 OE
                 22.5792MHz ──22Ω──┐
                                    ├──▶ XMOS MCLK输入 ──▶ 分频 ──▶ BCK/WS
                 24.576MHz ──22Ω──┘
XMOS GPIO_B ──▶ CCHD-957 #2 OE

GPIO_A和GPIO_B互斥：同一时刻只有一颗晶振输出有效。

晶振到XMOS：<15mm，同层，最少过孔
22Ω串联电阻抑制HCMOS边沿振铃
两颗晶振的MCLK输出在PCB上汇聚后进入XMOS（短走线线或合并）
未被使能的晶振输出三态（高阻），不影响有效时钟信号
```

### 4.3 固件配置

```c
#define MASTER_CLOCK_GENERATED       0     // 外部MCLK
#define I2S_MODE                     I2S_MODE_I2S
#define I2S_CHANS_PER_FRAME          2
#define STREAM_FORMAT_OUTPUT_RESOLUTION  16  // 16-bit
#define MCLK_BCK_RATIO               ???   // 需确认：实现32fs的具体配置
// 目标：BCK = fs × 32（每声道16个BCK，无填充）
// XMOS开源固件中此参数的确切名称和值需在阶段一验证
#define MAX_FREQ                     192000 // 结构上可支持192kHz
```

**32fs配置的验证优先级最高**——阶段一第一个任务就是确认XMOS固件
能输出32fs帧格式。如果XMOS开源固件不支持32fs，备选方案：
- 修改固件源码（XMOS开源固件可改）
- 或回退到64fs + 最高96kHz（降级但可用）

### 4.4 USB保护

```
D+/D-：USBLC6-2 ESD + 共模扼流圈
USB Shield：100Ω // 1nF → SGND；直连 → CGND
```

### 4.5 关于XMOS内部PLL

与v2.1相同的立场：不在文档层面断言"PLL不在I2S路径上"。
用J-test边带和相噪测量实测确认。

---

## 5. 模拟输出级

### 5.1 设计约束

TDA1543运行在+5V。OPA1656 I/V级独立运行在+9V（独立LT3042 #3）。
TDA1543输出为电流，需要I/V转换。
I/V运放输出带有4.5V附近的直流偏置，需AC耦合。

### 5.2 TDA1543输出特性

```
TDA1543电流输出（单颗）：
  满幅输出电流：约 ±0.55mA（典型，以实测为准）
  输出直流偏置电流：有，取决于内部偏置
  输出阻抗：高（电流源特性）

2并联后（每声道）：
  满幅输出电流：约 ±1.1mA（2 × 单颗）
  噪声：√2 × 单颗（不相关叠加）
  SNR提升：+3.0dB

关键参数需实测确认：
□ 单颗满幅输出电流幅度
□ 2颗并联后电流叠加的线性度（理论上完美线性，实测确认）
□ 输出直流偏置电压（在I/V后测量）
□ 并联后idle tone是否减少（DNL平均效果）

Compliance voltage（电流源线性区）：
  老式电流输出DAC的Iout引脚对其端电压有隐含的线性区范围。
  I/V运放的虚地将Iout拉到偏置点电压（v3.4.2默认4.5V）——
  TDA1543 VDD=7V，Iout在4.5V，距合规上限VDD−1.2V=5.8V有1.3V裕量。
  Datasheet明确标注output voltage compliance(DC) = 1.8V ~ VDD−1.2V。
  电流源headroom = 7.0−4.5 = 2.5V，处于健康工作区间。
  合规性由设计保证——不依赖Gate-2实验来决定"能不能用这个偏置"。
  Gate-2仍记录此工作点的实际THD作为基线数据。
```

### 5.3 I/V转换

```
                     Rf (2.5kΩ默认)
                   ┌────/\/\/──────────┐
                   │                   │
                   │       Cf          │
                   │   ┌───┤├───┐      │
                   │   │  (可调) │      │
TDA1543 ×2 ──Rs───┼───┴────────┴──────┼──────▶ Vout
Iout并联    预留   │        │          │
(±1.1mA)  10-33Ω  │      ┌─┴─┐        │
                   │     ╱  -  ╲       │
                   │    │ OPA   │──────┘
                   │    │ 1656  │  V+ = +9V_IV
                   │     ╲  +  ╱
                   │      └─┬─┘
                   │        │
                   └────────┘
                         │
                    4.5V偏置（from +9V_IV分压）
```

**运放：OPA1656**

| 参数 | 值 |
|------|-----|
| 供电 | **单+9V**（独立LT3042 #3） |
| 输出 | Rail-to-rail（距电源轨250mV，即0.25V~8.75V @2kΩ负载） |
| GBW | 28 MHz |
| 输入噪声 | 3.8 nV/√Hz |
| THD+N | -128 dB (1kHz) |
| 封装 | SOIC-8 单通道 |
| 输入偏置 | CMOS，极低偏置电流 |

为什么OPA1656而不是OPA1611（v2.1选择）：
- OPA1611输出不是rail-to-rail，+9V单电源下也只能到~2.0V~7.0V
- OPA1656 rail-to-rail输出覆盖0.25V~8.75V（@2kΩ负载），headroom充足

**为什么OPA1656独立+9V供电（v3.4.2核心变更）：**
- +5V单电源下OPA1656最大安全输出≈1.2Vrms（距标准2Vrms差4.4dB）
- 常规耳放/功放输入灵敏度在2Vrms左右，1.2Vrms输出意味着后级永远跑不满
- +9V打开天花板→Rf=2.5kΩ可输出~1.94Vrms，接近标准线电平
- TDA1543只需+5V（内部基准参考+5V），独立供电互不影响
- 附加收益：DAC↔I/V电源隔离，消除+7V_DAC上R2R开关噪声对I/V级的调制
- 代价：仅增加一颗LT3042 + 去耦（~$7），信号路径不变

**同相输入偏置（+端）：**

```
方案B（沿用）：电阻分压 + 大电容滤波，电源源改为+9V_IV

+9V_IV ── R1 (10kΩ) ──┬── OPA1656 (+)
                          │
GND ── R2 (10kΩ) ────────┘
                          │
                    47μF电解 // 2.2μF PP薄膜 // 100nF C0G ── GND

三级旁路（信号I/V偏置节点）：
  47μF电解：直流稳定+低频旁路
  2.2μF PP薄膜（DA≤0.05%）：覆盖1Hz~10kHz，抑制电解DA释放
  100nF C0G：高频旁路

为什么需要PP薄膜层：
  电解电容DA ≈ 1-5%，释放时间常数100ms~数秒
  大信号段偏置电容被充放电 → 衰减段电解DA释放残留电荷
  → 偏置点缓慢漂移 → 运放工作点在衰减过程中微移
  → 尾韵衰减中失真特性渐变（"呼吸感"）
  PP在DA释放对应的频段（0.3~10Hz）提供低阻低DA旁路，
  吸收电解DA释放的电荷，保持偏置点在衰减段稳定

dummy偏置节点保持47μF电解+100nF C0G（不加PP）：
  dummy通道DA影响通过二阶路径耦合，影响极小

分压：+9V × 10/(10+10) = 4.50V
R1∥R2 = 5.0kΩ → 5.0kΩ × 47μF → fc = 0.68Hz → 20Hz处衰减 > 29dB
两声道各自独立的分压网络
```

**选择方案B**（电阻分压+大电容）。理由与v3.4相同：
- 不增加IC
- 噪声通过47μF充分滤除
- 电源源从+5V_A改为+9V_IV，偏置设为4.5V（+9V对称中点）
- 4.5V偏置点让输出在+9V单电源下正负半周完全对称

**偏置点灵活性（为Gate-2预留）：**
- 默认：R1=10kΩ / R2=10kΩ → 4.50V from +9V（天然匹配，完美对称）
- PCB上臂电阻位预留两个串联0603焊盘，可灵活组合阻值
- 退档方案：R1=12.7kΩ / R2=10kΩ → 3.96V from +9V
  TDA1543合规裕量增加0.5V，OPA1656对称性略降但仍安全
- 终极回退：PCB预留跳线位，OPA1656 V+可切换到+7V_DAC
  R1=10kΩ / R2=15.7kΩ → 4.30V from +7V

### 5.4 电平预算

```
2并联后每声道满幅输出电流：±1.1mA（2 × 0.55mA，待实测确认）
OPA1656供电：+9V_IV（独立LT3042 #3）
摆幅极限：0.25V ~ 8.75V（距轨250mV @2kΩ负载）

═══════════════════════════════════════════════════
默认配置：偏置4.5V + Rf=2.5kΩ → 1.94Vrms
═══════════════════════════════════════════════════

Rf = 2.5kΩ：
  Vpeak = 1.1mA × 2.5kΩ = 2.75V
  Vrms = 2.75 / √2 = 1.94 Vrms

  I/V输出直流偏置：4.5V（+9V_IV分压器设定）
  输出摆幅：4.5V ± 2.75V = 1.75V ~ 7.25V
  Headroom低端：1.75V - 0.25V = 1.50V ✓（充裕）
  Headroom高端：8.75V - 7.25V = 1.50V ✓（充裕，完美对称）

  TDA1543 compliance：Iout引脚端电压=4.5V，VDD=7V
  合规上限VDD−1.2V=5.8V，裕量1.3V ✓
  电流源headroom=2.5V ✓

Rf = 2.2kΩ（第一退档，如实测IFS偏低需要降Rf）：
  Vpeak = 1.1mA × 2.2kΩ = 2.42V
  Vrms = 2.42 / √2 = 1.71 Vrms

  输出摆幅：4.5V ± 2.42V = 2.08V ~ 6.92V
  Headroom低端：2.08V - 0.25V = 1.83V ✓（更充裕）
  Headroom高端：8.75V - 6.92V = 1.83V ✓（对称）

Rf = 2.0kΩ（最保守）：
  Vpeak = 1.1mA × 2.0kΩ = 2.2V
  Vrms = 2.2 / √2 = 1.56 Vrms

  输出摆幅：4.5V ± 2.2V = 2.3V ~ 6.7V
  Headroom低端：2.3V - 0.25V = 2.05V ✓（宽裕）
  Headroom高端：8.75V - 6.7V = 2.05V ✓（对称）

═══════════════════════════════════════════════════
退档方案（如Gate-2需要）
═══════════════════════════════════════════════════

  退档1 — Rf=2.2kΩ（1.71Vrms）：headroom增至1.83V/1.83V
  退档2 — 偏置4.0V + Rf=2.5kΩ（1.94Vrms）：合规裕量增至1.8V
  终极回退 — OPA1656切+7V_DAC，偏置4.3V + Rf=2.2kΩ（1.71Vrms）

选择策略：
  默认Rf = 2.5kΩ（0.1%精度，薄膜，TCR≤25ppm/°C），偏置4.5V → 1.94Vrms
  → Gate-2判定：0dBFS THD比-6dBFS差>3dB → 退档1
  → 仍不理想 → 退档2
  → +9V方案整体不可行 → 终极回退

PCB Rf策略：
  0805焊盘预留，支持2.0kΩ / 2.2kΩ / 2.5kΩ三档。

如果实测TDA1543满幅电流 ≠ 0.55mA：
  需按实测值重算Rf。PCB三档预留覆盖此不确定性。

对比各版本输出电平：
  v3.3 单颗 +5V：   0.78Vrms（-8.2dB ref 2Vrms）
  v3.4 2并联 +5V：  1.17Vrms（-4.7dB）
  v3.4.2 2并联 +9V：1.94Vrms（-0.3dB）← 基本达标
```

### 5.5 Cf与Rs

```
Cf（反馈电容）：
  双并联焊盘（0603）
  焊盘1：固定10pF
  焊盘2：可调0~47pF
  默认：22pF

Rs（输入串联电阻预留）：
  PCB预留0603焊盘，默认0Ω短接
  目的：隔离TDA1543输出端寄生电容与运放反相端输入电容的谐振，
  改善I/V环路在高频的相位裕度。
  属于"只做必要的事"：默认不装（0Ω），
  只有在示波器证明存在高频振铃或振荡边界时才替换为10~33Ω。
  装入后需重新评估Cf值（Rs+Cf共同决定高频极点位置）。

运放去耦（紧贴引脚）：
  V+ (+9V_IV) : 10μF MLCC (≤3mm) + 100nF X7R (≤1mm)
  接地引脚旁：100nF 到GND
```

### 5.6 I/V稳定性调试

```
□ 10kHz方波输入 → 观察振铃 → 调整Cf
□ 阶跃 + 1nF容性负载 → 确认不振荡
□ 不同电池电压区间测试（10.95V / 9.0V / 7.5V）
  LDO压差和PSRR随Vin变化，影响稳定性边界
□ 空载 vs 1m RCA线缆负载 → 确认无条件稳定
```

### 5.7 低通滤波器

四阶无源Bessel，fc ≈ 70kHz。由两段二阶Bessel section级联构成。

**为什么NOS DAC必须认真对待超声滤波：**

NOS（无数字滤波）DAC的R2R直出零阶保持阶梯波，频谱中天然存在镜像分量。
对任意音频频率f，fs=44.1kHz采样下的第一组镜像为：

```
下侧镜像：fs - f
上侧镜像：fs + f

例：f=15kHz → 镜像在 29.1kHz 和 59.1kHz
例：f=19kHz → 镜像在 25.1kHz 和 63.1kHz
例：f=20kHz → 镜像在 24.1kHz 和 64.1kHz
```

如果后级存在二次非线性（管机的典型特征），音频基波f与其下侧镜像(fs-f)的差频为：

```
差频 = (fs - f) - f = fs - 2f

这条公式揭示了最危险的场景：
  f = 10kHz → 差频 = 44.1 - 20 = 24.1kHz（超声，不可闻）
  f = 15kHz → 差频 = 44.1 - 30 = 14.1kHz（可闻，但不在敏感区）
  f = 18kHz → 差频 = 44.1 - 36 = 8.1kHz（敏感区）
  f = 19kHz → 差频 = 44.1 - 38 = 6.1kHz（高度敏感区）
  f = 20kHz → 差频 = 44.1 - 40 = 4.1kHz（人耳最敏感的频段）

结论：越接近带宽上沿的音频内容，其镜像IMD产物越落在人耳敏感区。
```

Roger DAC的后端是Sennheiser HEV90（管放）+ HE90（静电耳机）：
- HEV90：管子二次非线性远高于晶体管，超声→音频带交调风险是真实的
- HE90：静电振膜质量极轻，高频延伸>40kHz，瞬态分辨率极高——
  任何时域瑕疵和带内杂散都会被暴露

这个系统把NOS的超声残留从"理论问题"变成"系统级可闻风险"。
二阶Bessel在44.1kHz处只有~12dB衰减——不够。
升到四阶，同频率衰减~24dB，IMD产物随之降低约12dB，大概率推到不可闻门槛以下。

**为什么四阶Bessel而不是其他方案：**

| 方案 | 44.1kHz衰减 | 20kHz处通带损耗 | 时域代价 |
|------|------------|----------------|---------|
| 二阶Bessel fc=70k | ~12dB | <0.5dB | 群延迟变化<5μs，最小振铃 |
| 二阶Bessel fc=50k | ~18dB | ~1.0dB | 开始削弱HE90的高频延伸 |
| **四阶Bessel fc=70k** | **~24dB** | **<0.5dB** | **群延迟变化<10μs，同阶最小振铃** |
| 四阶Butterworth fc=70k | ~24dB | <0.3dB | 阶跃响应有可见overshoot |
| 数字FIR（oversampling） | >100dB | 0dB | pre-ringing——Roger DAC拒绝的 |

四阶Bessel是"时域代价最小的镜像抑制增益方式"。

关于阶跃响应的精确措辞：
Bessel滤波器的定义是群延迟最平坦（maximally flat group delay）。
这意味着它在同阶滤波器中拥有最好的瞬态保真和最小的振铃倾向。
二阶Bessel的阶跃响应实际上无可见overshoot。
四阶Bessel的阶跃响应在理想条件下overshoot极小（<1%），
但在元件公差和实际负载下可能略有偏差——
验收标准定为"示波器无可见振铃"而不是"零overshoot"。

**电路拓扑：两段二阶RC梯形网络级联**

```
        Section 1 (2nd-order)          Section 2 (2nd-order)
          R1a          R1b               R2a          R2b
Vin ──/\/\/──┬──/\/\/──┬────────/\/\/──┬──/\/\/──┬──────▶ Vout
              │          │                │          │
             C1a        C1b              C2a        C2b
              │          │                │          │
             GND        GND              GND        GND
```

每个二阶section由2个电阻+2个电容组成（RC梯形网络，2个极点）。
两段级联 = 4个极点 = 四阶。
总元件：4个电阻 + 4个电容 = 8个被动元件。

注意：单个RC（1R+1C）只能实现一阶（1个极点）。
二阶无源需要至少两个储能元件（两只电容），形成梯形网络。
四阶 = 两段二阶梯形级联，不是"四个一阶RC"简单串联——
简单串联会因级间负载效应导致极点偏移，无法精确实现Bessel多项式。

**级间阻抗设计：**

```
Section 1的输出阻抗 = R1a + R1b（电容在高频短路后）
Section 2的输入阻抗取决于R2a和C2a

原则：Section 1的输出阻抗应远小于Section 2的输入阻抗，
      或在设计时将级间负载效应纳入计算。

实用做法：
  按四阶Bessel归一化多项式查表，得到归一化元件值
  选择阻抗水平时兼顾：
  - 不能太高（与后级10μF耦合电容和线缆电容交互）
  - 不能太低（I/V运放驱动负载增大，影响线性度）
  - 典型阻抗水平：数百Ω到数kΩ

具体元件值需LTspice仿真确认。
仿真时必须包含真实负载模型（见下方仿真要求）。

元件材质要求：
  电阻：薄膜（thin film），TCR ≤ 25ppm/°C
  电容：聚丙烯（PP）薄膜优先——无压电效应，无介质吸收
        体积不允许时可用C0G/NP0 MLCC（压电效应低于X7R一个数量级，但非零）
        不可用X7R/X5R（电压系数±15% → 信号相关电容变化 → 可测量的谐波失真）
```

**验收标准：**

通带与群延迟（LTspice仿真 + 实测双重确认）：

| 指标 | 验收值 | 说明 |
|------|--------|------|
| 20Hz~20kHz通带平坦度 | < ±0.3 dB | 音频带内不着色 |
| 20kHz处衰减 | < 0.5 dB | 保护HE90高频延伸 |
| 20Hz~20kHz群延迟变化 | < 10 μs | 四阶Bessel特征保持 |
| 阶跃响应 | 示波器无可见振铃 | 时域优先的核心验收（不承诺"零overshoot"） |

镜像频点抑制（以fs=44.1kHz为基准，相对1kHz处电平）：

| 频点 | 物理含义 | 验收值 |
|------|---------|--------|
| 24.1kHz (fs-20k) | 20kHz音频的下侧镜像，差频落在4.1kHz | ≥ 12 dB |
| 25.1kHz (fs-19k) | 19kHz音频的下侧镜像，差频落在6.1kHz | ≥ 14 dB |
| 29.1kHz (fs-15k) | 15kHz音频的下侧镜像，差频落在14.1kHz | ≥ 18 dB |
| 44.1kHz (fs) | 采样频率中心 | ≥ 24 dB |
| 88.2kHz (2fs) | 第二镜像 | ≥ 40 dB |

这组指标直接对应"镜像与音频交调后落入可听带的风险频点"。

系统级IMD验收（在完整链路 DAC→HEV90→HE90端电压 上测量）：

```
测试方法：双音输入
  □ 测试信号对：15kHz + 19kHz（或18kHz + 19kHz），等幅，-6dBFS
  □ 在HEV90输出端（或HE90端电压）频谱分析
  □ 观察音频带内（20Hz~20kHz）新产物
  □ 对比四阶LPF vs 无LPF（直连）的差异

验收标准：
  □ 四阶LPF下，音频带内IMD产物低于底噪+10dB
  □ 或相对基波 > 80dB抑制

这条验收直接证明"升阶LPF确实把系统级IMD风险压到安全线以下"——
比纸面推导更有说服力。
```

**LTspice仿真必须包含真实负载模型**：

```
仿真链路：
  I/V输出（低阻）→ 四阶Bessel LPF → 10μF PP薄膜 → 47Ω → 1m RCA (~200pF) → 后级 (10kΩ // 100pF)

关注指标：
□ 20Hz~20kHz通带平坦度
□ 镜像频点族的实际衰减（对照上表）
□ 群延迟特性在实际负载下是否保持Bessel形态
□ 负载电容对各section阻尼的影响
□ 阶跃响应——确认无可见振铃
```

### 5.8 输出

```
I/V输出 ──▶ 四阶Bessel LPF ──▶ 10μF PP薄膜 ──┬── 47Ω ──┬──▶ RCA热端
                     (去除4.5V     │         │
                      直流偏置)     │       100pF (EMI)
                                   │         │
                                  GND       GND

输出电平：~1.94 Vrms（Rf=2.5kΩ，偏置4.5V，+9V供电时）
输出阻抗：~47Ω + LPF串联电阻

耦合电容必须是聚丙烯（PP）薄膜：
  介质吸收（DA）：PP ≈ 0.03-0.05%，聚酯 ≈ 0.3-0.5%
  DA让电容"记住"之前的信号电压 → 瞬态后产生残留 → 时域涂抹
  在Orpheus级别的系统分辨率下这个差异可能可闻
  PP体积较大（典型~18×11×5mm），但只有2颗，PCB可容纳
  不可用聚酯，不可用电解
```

无独立输出缓冲级——I/V运放（OPA1656）输出阻抗<1Ω。
四阶Bessel LPF的串联电阻总和取决于仿真确定的元件值（典型数百Ω到数kΩ），
加上47Ω输出电阻，构成实际输出阻抗。
后级HEV90输入阻抗>>10kΩ，不构成负载问题。
但LPF阻抗水平的选择需在仿真中兼顾：
- 不能太高：与10μF耦合电容形成低频截止偏移，与线缆电容交互影响高频
- 不能太低：I/V运放驱动电流增大
PCB预留缓冲运放焊盘位，如果LPF输出阻抗过高影响线缆驱动能力，可加回。

---

## 6. 电源系统

### 6.1 完整电源树

```
3S LiFePO4 (7.5~10.95V)
│
├──▶ [A] LT3042 #1 ──▶ 3.3V_CLK ──▶ 晶振×2
│         超低噪声 (0.8μVrms)
│
├──▶ [B] LT1763 #2 ──▶ +7V_DAC ──▶ TDA1543 VDD ×4
│         超低噪声
│
├──▶ [B2] LT3042 #3 ──▶ +9V_IV ──▶ OPA1656 V+ ×2
│          超低噪声                ──▶ I/V偏置分压网络 ×2
│                                  ──▶ dummy偏置分压网络 ×4
│
└──▶ [C] LT1763 ──▶ 3.3V_D ──▶ XMOS VDD + VDD_IO
          中等噪声 (20μVrms)
```

**四路LDO，无负电源，无电荷泵，无基准电压IC。**

TDA1543和OPA1656各自独立的超低噪声LT3042供电——
DAC不被I/V级的电流尖峰调制，I/V级不被DAC的R2R开关噪声调制。
独立+9V_IV是v3.4.2的核心改动：输出电压提升 + 电源隔离，一颗LT3042同时解决两个问题。

### 6.2 为什么不需要单独的Vref

v3.0离散R2R需要ADR4525提供精密Vref（输出电压 = Vref × 数字码 / 2^N）。
TDA1543内部有自己的基准——它的R2R网络参考内部偏置。
外部只需提供干净的VDD，内部基准自理。
所以ADR4525被消除了。

### 6.3 各路详细设计

#### [A] 3.3V_CLK — 晶振专用

```
电池 ── 磁珠(1kΩ@100M) ──┬── 10μH ──┬── LT3042 ──┬──▶ 3.3V_CLK
                           │          │   (SET=     │
                          10μF      100nF  53.6kΩ)  ├── 100nF C0G ── GND
                          MLCC       +10μF          ├── 10nF  C0G ── GND
                           │          │             │
                          GND        GND        ┌───┴───┐
                                                │       │
                                             22.5M   24.5M
                                             晶振     晶振

LT3042到晶振：< 5mm
晶振旁路：C0G/NP0
```

#### [B] +7V_DAC — DAC专用

```
电池 ── 磁珠(600Ω@100M) ──┬── LT1763 ──┬──▶ +7V_DAC
                            │  (adj,      │
                           10μF  SET=     ├── 10μF MLCC ── GND
                           MLCC  121kΩ)   │
                            │             ▼
                           GND     TDA1543 VDD ×4

为什么VDD=7V而不是5V（v3.4.2核心变更）：
  Datasheet VDD工作范围：3.0V ~ 8.0V（绝对最大9V）
  Iout合规电压(DC)：1.8V ~ VDD−1.2V
  
  VDD=5V + 偏置3.5V → 合规裕量仅0.3V（距VDD−1.2V=3.8V）← 危险
  VDD=7V + 偏置4.5V → 合规裕量1.3V（距VDD−1.2V=5.8V）✓
  电流源headroom = 7.0−4.5 = 2.5V（接近Datasheet典型应用的2.8V）
  
  附加收益：
  4.5V偏置 = +9V供电OPA1656的精确中点
  → 信号正负半周完美对称 → THD对称 → 无方向性失真签名
  10kΩ/10kΩ分压 = 天然匹配 → 偏置精度更高

为什么LT1763而不是LT3042：
  4颗TDA1543 @7V：每颗~52mA → 总计~208mA
  LT3042额定200mA → 略超
  LT1763额定500mA → 充裕
  PSRR：LT1763 @1kHz约60dB（vs LT3042的79dB）
  但TDA1543自身PSRR=50dB → 总抑制60+50=110dB → 足够

负载预算：
  TDA1543 ×4 @7V：~52mA ×4 = ~208mA
  LT1763热耗：(9.6 − 7.0) × 0.208 = 0.54W

散热（**必做**，与3.3V_D的LT1763共享散热策略）：
  LT1763 Tab焊盘下方：PCB铜皮 ≥ 2.0cm²
  热过孔阵列：3×3，0.3mm孔径，连接到内层/底层铜皮
  布局：远离模拟核心区

TDA1543 DIP-8散热（VDD=7V新增关注）：
  每颗功耗：7V × 52mA = 364mW
  DIP-8 Rth(j-a) = 100K/W（自由空气）
  密封铝壳内环境温度约38°C（系统总热耗全由壳体散出）
  优化散热路径：GND引脚(pin4,5) → PCB铜皮 → 热过孔 → Bottom铜皮 → 导热垫 → 铝壳
  优化后Rth ≈ 70K/W
  结温：364mW × 70K/W + 38°C ≈ 64°C（限值85°C，裕量21°C）✓

电池约束：
  LT1763 dropout ~0.3V → 输入最低需 7.3V
  3S LiFePO4红灯（9.0V）时headroom = 9.0−7.0 = 2.0V → 充裕
  BMS切断7.5V时headroom = 0.5V → 仍可维持
```

#### [B2] +9V_IV — I/V运放+偏置专用（v3.4.2新增）

```
电池 ── 磁珠(600Ω@100M) ──┬── LT3042 ──┬──▶ +9V_IV
                            │  (SET=      │
                           10μF  180kΩ)   ├── 10μF MLCC ── GND
                           MLCC           │
                            │         ┌───┴──────────────┐
                           GND        │                  │
                                      ▼                  ▼
                               OPA1656 V+ ×2    偏置分压网络 ×6
                                                (信号×2 + dummy×4)

负载预算：
  OPA1656 ×2：静态 ~1.8mA ×2 = 3.6mA
  信号I/V偏置 ×2：+9V/(10k+10k) ×2 = 0.45mA ×2 = 0.9mA
  dummy偏置 ×4：+9V/(10k+10k) ×4 = 0.45mA ×4 = 1.8mA
  总计：~6.3mA
  LT3042热耗：(10.95 - 9) × 0.006 = 0.012W（忽略不计）

电池约束：
  LT3042 dropout ~350mV → 输入最低需 9.35V
  3S LiFePO4平坦区 ~9.6V，放电末段（<20%容量）降到~9.3V
  实际影响：LT3042在dropout区仍能输出~8.9V，headroom仅减少~0.1V
  不影响正常使用。

为什么不继续共享+7V_DAC：
  1. +7V下OPA1656偏置受限，难以同时满足合规和对称性
  2. +9V打开天花板 → 偏置4.5V完美居中 → Rf=2.5kΩ输出1.94Vrms
  3. DAC与I/V电源完全隔离，R2R开关噪声不调制I/V级
  4. 成本极低：+1颗LT3042 + 去耦 ≈ +$7
```

#### [C] 3.3V_D — 数字

```
电池 ── LT1763 ──┬──▶ 3.3V_D ──▶ XMOS全部电源引脚
        (adj)     │
                  ├── 10μF MLCC + 100nF ── GND
                  │
                 GND

负载：XMOS ~120mA
Pdiss_max = (10.95 - 3.3) × 0.12 = 0.92W
```

**两颗LT1763是v3.4.2的现实热问题**——在90×60mm板上不可忽视。

| LDO | Vout | Iload | Pdiss(max) |
|-----|------|-------|-----------|
| LT1763 #2 (DAC) | +7V | 208mA | 0.82W |
| LT1763 (数字) | 3.3V | 120mA | 0.92W |
| 合计 | | | **1.74W** |

散热措施（**必做，不是可选**）：
- LT1763 Tab焊盘下方：PCB铜皮 ≥ 2.0cm²
- 热过孔阵列：3×3（最少），0.3mm孔径，连接到内层铜皮
- LT1763布局：远离模拟核心区（TDA1543/OPA1656），避免热辐射影响I/V偏置稳定性
- Layer 3电源平面的3.3V_D区域：尽可能大面积铜皮辅助散热

**可选工程分支（如Gate-4温升不通过则必须启用之一）：**

```
方案D1：预降压（最少改动）
  电池 ── TPS62160（降压到~5V）── LT1763 ── 3.3V_D
  目的：把LT1763压差从7.65V降到1.7V
  新Pdiss = (5.0 - 3.3) × 0.12 = 0.20W（降低77%）
  代价：增加1颗DC-DC + 电感 + 去耦
  注意：DC-DC开关噪声必须被LT1763充分抑制
  TPS62160布局远离模拟区，输入侧加磁珠隔离

方案D2：独立2S电池供数字
  2S LiFePO4 (5.0~7.3V) ── LT1763 ── 3.3V_D
  Pdiss_max = (7.3 - 3.3) × 0.12 = 0.48W（降低48%）
  优势：物理隔离更彻底
  代价：结构复杂（两套电池/BMS/充电）
```

### 6.4 电源分配表

| 编号 | LDO | Vout | 负载 | Iload | Pdiss(max) | 散热 |
|------|-----|------|------|-------|------------|------|
| A | LT3042 #1 | 3.3V | 晶振×2（同时只有1颗工作） | 25mA（单颗最坏估算） | 0.19W | 标准铜皮 |
| B | LT1763 #2 | +7V | TDA1543×4 | 208mA | **0.82W** | **必做加强散热** |
| B2 | LT3042 #3 | +9V | OPA1656×2 + 偏置×6 | 6mA | 0.01W | 标准铜皮 |
| C | LT1763 | 3.3V | XMOS | 120mA | **0.92W** | **必做加强散热（Gate-4）** |

注：CCHD-957电流按最坏25mA/颗估算（公开资料范围15~25mA，按上限取）。
OE互斥控制保证同一时刻只有一颗晶振工作。
TDA1543 IDD：Datasheet标注50mA typ @5V（每颗，含双声道），7V下约52mA。
Pdiss(max)按满充电压10.95V计算。

总LDO功耗：~1.94W
系统总电池消耗：9.6V × (25+208+6+120)mA ≈ 3.45W（电池标称电压）
电池2000mAh × 9.6V（平坦区中值）= 19Wh/组
理论续航：19Wh / 3.45W ≈ 5.5小时（实际按4~5小时估计，含LDO效率和放电曲线损耗）
单组，红灯（9.0V）后仍有~1小时余量完成当前session

注：续航从早期估算的12~14小时大幅修正——
  原估算错误地使用了TDA1543的16mA（LDO静态消耗），
  实际TDA1543 IDD=50mA/颗×4=200mA，占系统总消耗的58%。
  4~5小时仍足够1~2个完整聆听session。

### 6.5 TDA1543电源去耦

TDA1543对VDD噪声敏感——VDD噪声直接调制内部基准。
4颗芯片共享+7V_DAC，每颗独立去耦尤为重要。

```
每颗TDA1543的VDD去耦：

+7V_DAC主干 ──┬── 10μF MLCC (≤3mm到VDD引脚)
             │
             ├── 100nF X7R (≤1mm到VDD引脚)
             │
             └── 10nF C0G (≤1mm到VDD引脚，高频旁路)
                 ↑ 这颗C0G是关键——TDA1543的R2R开关动作
                   会在VDD上产生高频尖峰，C0G吸收

GND引脚去耦：
  引脚4和引脚5都是GND
  两个引脚都要独立过孔到Layer 2地平面
  过孔尽量靠近引脚（<1mm）
```

### 6.6 电池与电源管理

```
单组3S LiFePO4 18650 ×3
三档拨杆开关（DP3T，上下拨动，前面板垂直安装）

档位布局（上→下）：

  ▲ OFF    ── 全部断开，完全关机
  ● ON     ── 电池 → DAC供电，DC jack断开
  ▼ CHARGE ── 电池 → 外部充电器（经DC jack），DAC断电

为什么这个次序：
  OFF在最上方：默认/安全位置（运输、存放时的自然停靠档）
  ON在中间：日常使用最频繁的档位，上下都只需拨一档
  CHARGE在最下方：充电时设备静置桌面，拨到底部自然

三档拨杆的核心价值：
  物理层面强制了"不边充边放"——ON和CHARGE不可能同时激活。
  不依赖用户记忆或软件逻辑，机械结构保证电气隔离。
  这比文档写"不做边充边放"然后依赖用户行为可靠得多。

电路拓扑：

  DP3T = 两组三掷开关联动（双刀三掷）

  第一刀（电源通路）：
    OFF：断开
    ON：电池+ → 10Ω限流 → 100μF软启动 → LDO链输入
    CHARGE：电池+ → BMS充电输入 ← DC jack ← 外部充电器

  第二刀（LED/状态控制）：
    OFF：断开（LED灭）
    ON：电池分压 → LM393比较器A → 双色LED（绿=正常，红=低电量）
    CHARGE：电池分压 → LM393比较器B → 双色LED（慢闪红=充电中，慢闪绿=满）

  拓扑图：

  电池+ ──┐
           │         DP3T 第一刀
           ├── OFF ── (断开)
           ├── ON  ── 10Ω ── 100μF ──→ LDO链输入（+9V_IV, +7V_DAC, 3.3V_CLK, 3.3V_D）
           └── CHG ── DC jack ←── 外部3S充电器
                       │
                      BMS充电路径

  电池分压 ─┐
            │         DP3T 第二刀
            ├── OFF ── (断开，LED灭)
            ├── ON  ──→ LM393-A（电量比较）──→ 双色LED
            └── CHG ──→ LM393-B（充电状态+慢闪振荡）──→ 双色LED

ON模式 — 电量指示：
  电池电压经分压（100kΩ/33kΩ → 满电10.95V映射~2.72V）
  LM393比较器A：
    Vbatt_div > Vref(9.0V映射) → 绿灯常亮
    Vbatt_div < Vref(9.0V映射) → 红灯常亮
  
  9.0V阈值：3S LiFePO4平坦区~9.6V，9.0V约放电末段20%位置
  红灯亮后仍有~1小时余量完成当前session
  BMS欠压保护在~7.5V切断——红灯到切断之间有充分缓冲
  用户看到红灯 → 听完当前session → 拨到CHARGE → 下次使用前充满

CHARGE模式 — 充电状态指示：
  不上板载充电IC——使用外部3S LiFePO4充电器（BOM已有，$15）
  充电器接DC jack → DP3T第一刀将DC jack连到BMS充电输入
  
  充电状态检测：
    LM393比较器B检测电池电压：
      Vbatt_div < Vref(10.8V映射) → 充电中 → 红灯慢闪
      Vbatt_div ≥ Vref(10.8V映射) → 已满 → 绿灯慢闪
    
    10.8V阈值：3S LiFePO4满充10.95V，
    10.8V以上充电器进入CV涓流阶段，基本充满

  慢闪实现：
    LM393比较器B输出端RC正反馈振荡
    R=1MΩ + C=1μF → 周期约2秒（0.5Hz慢闪）
    不额外加555——LM393双比较器足够：
    ON模式用比较器A（电量检测，常亮），
    CHARGE模式用比较器B（充电状态+振荡，慢闪）
    两个比较器通过DP3T第二刀切换接入同一颗双色LED

不做边充边放（机械强制）：
  ON时：第一刀接电池→LDO，DC jack无电气连接
  CHARGE时：第一刀接电池→BMS←DC jack，LDO链无电气连接
  两个状态在DP3T内部物理隔离——不是电路设计，是机械结构

充电时间：
  18650 × 3 ≈ 19Wh
  外部充电器1A充电约6-7小时，2A约3-4小时

循环寿命：
  LiFePO4 18650：2000+次（到80%标称容量）
  80%容量时续航仍有 4.5h × 0.8 ≈ 3.6小时
  按每周3次session、每次2小时：年循环~48次 → 40+年到2000次
  不构成任何实际约束

续航：
  19Wh / 3.45W ≈ 5.5小时理论（实际4~5小时）
  红灯亮（~9.0V）时仍有~1小时
  足够完成当前session后再充电

上电/关机保护：
  ON档：10Ω限流 + 100μF软启动，防LDO输入电容浪涌
  OFF档：LDO输出电容自然放电，无pop风险
  CHARGE档：DAC电路完全断电，无信号通路

元件清单（电源管理新增）：
  DP3T拨杆开关 ×1（~$3）
  LM393双比较器 ×1（~$0.3）
  分压电阻 100kΩ+33kΩ ×1组（~$0.1）
  基准分压（9.0V阈值+10.8V阈值）各1组（~$0.2）
  双色LED（共阴极）×1（~$0.2）
  RC振荡（1MΩ+1μF）×1组（~$0.1）
  10Ω限流 + 100μF软启动 ×1组（~$0.3）
  DC jack ×1（~$0.5）
  电源管理总计：~$5
```

### 6.7 机壳与物理布局

```
结构：薄铝折弯壳（Graham Slee风格）
材料：5052-H32铝板（折弯不易开裂）
厚度：0.8mm壳体，前后面板1.0mm（接口安装刚性）
工艺：激光切割 + 折弯（非CNC，非冲压模具）
表面：内表面裸铝（接缝导电接触），外表面可选拉丝或导电氧化

结构分件：
  U型底壳：底面 + 左右侧面折弯
  前后面板：1.0mm铝板，螺丝固定到U型壳
  平板顶盖：螺丝固定到底壳翻边

外尺寸：~100 × 70 × 30mm
内净空：~98 × 68 × 28mm

堆叠布局（侧视图）：

  ┌──────────────────────────────────────┐ ─ 顶盖 0.8mm
  │ [PCB Top面：元件，最高~6mm]          │
  │ [PCB 90×60mm, 1.6mm]                │ ~12mm
  │ [PCB Bottom面：平坦/热过孔]          │
  ├──────────────────────────────────────┤ ─ 绝缘膜 0.5mm
  │ ┌──────┐ ┌──────┐ ┌──────┐          │
  │ │18650 │ │18650 │ │18650 │          │ ~18mm（⌀18含固定）
  │ │ #1   │ │ #2   │ │ #3   │          │
  │ └──────┘ └──────┘ └──────┘          │
  └──────────────────────────────────────┘ ─ 底壳 0.8mm

电池排列（俯视图）：

  ┌─────────────────────────────────────┐
  │          PCB 90 × 60mm              │
  │                                     │ 70mm
  │  [18650#1] [18650#2] [18650#3]      │
  │  ←──── 54mm(3×⌀18) ────→  [BMS]   │
  │           ←──── 65mm ────→          │
  └─────────────────────────────────────┘
                   100mm

  电池长轴沿PCB长边方向
  3颗并排宽度54mm → 68mm壳内宽度有余量放固定结构
  电池长65mm + BMS板 → 100mm壳内长度足够
  25mm余量空间放BMS板和连接线

前面板（从左到右）：
  三档拨杆（上下拨动：OFF/ON/CHARGE）
  USB-C
  双色LED（电量/充电状态指示）

后面板：
  RCA × 2（L/R，间距25mm标准）
  DC jack（充电输入）

接地：
  底壳单点接到PCB信号地（后面板RCA安装孔处）
  USB Shield → CGND（不接信号地，见8.4节）

散热：
  LT1763布局在PCB边缘（电池排列区域之外的余量区）
  Tab → PCB铜皮 → 导热垫(0.5mm) → 底壳铝板
  底壳该区域下方无电池遮挡，直接对流散热
  底壳外表面散热区域不贴脚垫

电池固定：
  底壳内2道折弯隔板形成3个槽位
  或18650电池座（底壳固定型）
  可拆底壳更换电池

PCB安装（防microphonics）：
  M3螺丝 + 橡胶垫圈（不用金属螺柱直连壳体）
  隔离壳体振动到PCB的传递路径
  HE90是开放式耳机，声压直接外泄到桌面 → 桌面振动 → 壳体
  0.8mm铝板在100×70mm跨度有自身谐振 → 橡胶垫圈衰减传递
  信号路径上的MLCC（即使C0G）有残余压电效应，
  LPF电容优先选PP薄膜也是基于这个考虑

重量预算：
  壳体（0.8mm 5052铝，100×70×30）：~110g
  电池（18650 ×3）：~135g
  PCB + 元件：~30g
  总装：~275g
```

---

## 7. 时钟系统

### 7.1 晶振

Crystek CCHD-957 × 2：22.5792 MHz / 24.576 MHz
相位噪声：-118 dBc/Hz @ 1kHz offset
输出：HCMOS 3.3V
**OE引脚**：HIGH=输出有效，LOW=输出三态
OE控制：XMOS GPIO（3.3V直驱，无需电平转换）

OE引脚处理：
- OE引脚通过10kΩ下拉到GND（默认关闭，上电时两颗都不输出）
- XMOS GPIO推高OE → 使能对应晶振
- 确保上电瞬间不会两颗同时输出
- **上电安全序列**：XMOS GPIO在固件初始化完成前必须保持输入模式或低电平
  （10kΩ下拉保证此期间OE=LOW，即使GPIO浮空）

### 7.2 时钟电源

LT3042 #1专用。详见6.3 [A]。

### 7.3 晶振到XMOS走线

```
晶振输出 ──[22Ω]──▶ XMOS MCLK输入
            ↑
      抑制HCMOS边沿振铃
      减少地弹与串扰

走线规则：
  长度 < 15mm
  同一PCB层（Top）
  最少过孔（理想为0）
  远离数字总线和I2S走线
```

---

## 8. PCB设计

### 8.1 板层

```
Layer 1 (Top):     信号 + 元件
Layer 2 (Inner1):  完整地平面（不切割、不挖槽）
Layer 3 (Inner2):  电源平面
Layer 4 (Bottom):  信号 + 元件
FR4, 1.6mm, 1oz铜
```

### 8.2 布局

```
信号流方向 ══════════════════════════════════════════════════════▶

┌─────────────────────────────────────────────────────────────────┐
│                      约 90mm × 60mm                              │
│                                                                  │
│  ┌─────┐                                                        │
│  │ USB │  ESD+CMC                                               │
│  │     │────┐                                                   │
│  └─────┘    │                                                   │
│             ▼                                                   │
│     ┌──────────────┐                                            │
│     │    XMOS      │       I2S (3线广播)                        │
│     │   XU216      │─────────────────────────┐                  │
│     └──────────────┘                         │                  │
│            │                                 │                  │
│     ┌──────┴──────┐                          ▼                  │
│     │ 晶振×2      │               ┌────────────────────┐       │
│     │ +LT3042 #1  │               │   模拟核心区        │       │
│     └─────────────┘               │                    │       │
│                                   │  ┌──────┐ ┌──────┐│       │
│  ┌──────────┐                     │  │TDA   │ │TDA   ││       │
│  │ 电源区    │                     │  │1543  │ │1543  ││       │
│  │          │                     │  │#1(L) │ │#3(R) ││       │
│  │ LT3042#2│                     │  └──┬───┘ └──┬───┘│       │
│  │ LT3042#3│                     │  ┌──┴───┐ ┌──┴───┐│       │
│  │ LT1763  │                     │  │TDA   │ │TDA   ││  ┌────┐│
│  └──────────┘                     │  │1543  │ │1543  ││  │RCA ││
│                                   │  │#2(L) │ │#4(R) ││  │L/R ││
│  ┌──────────┐                     │  └──┬───┘ └──┬───┘│──│    ││
│  │ 电池接口 │                     │     │        │    │  └────┘│
│  │ DP3T     │                     │  OPA1656  OPA1656│       │
│  └──────────┘                     │   (L I/V)  (R I/V)│       │
│                                   │     │        │    │       │
│                                   │    LPF      LPF  │       │
│                                   │     │        │    │       │
│                                   │   镜像对称        │       │
│                                   └────────────────────┘       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

  底壳内（堆叠层，PCB下方）：
  ┌─────────────────────────────────────────────────────────────────┐
  │  [18650 #1]  [18650 #2]  [18650 #3]     [BMS] [LT1763散热区]  │
  │  ←────── 54mm(3×⌀18) ──────→  ←──25mm余量──→                  │
  └─────────────────────────────────────────────────────────────────┘
```

堆叠布局：PCB在上（元件朝上），电池在下（绝缘膜隔离）。
LT1763布局在PCB边缘（电池排列区域之外的25mm余量区），
Tab通过热过孔导到底面 → 导热垫 → 底壳铝板散热。

板面积约90mm × 60mm——4颗DIP-8 TDA1543比2颗多占约375mm²，
但总板面积5400mm²中仍有足够空间，不需要扩板。
壳体外尺寸~100×70×30mm，PCB位于上层，单组电池位于下方（详见6.7节）。

同声道的2颗TDA1543纵向紧邻排列，Iout引脚朝向同一侧的OPA1656。
Iout汇合走线尽可能短，对称（见8.3节详细要求）。

### 8.3 关键走线

| 信号 | 最大长度 | 说明 |
|------|----------|------|
| 晶振 → XMOS MCLK | <15mm | 22Ω串联，同层 |
| 时钟LDO → 晶振 | <5mm | C0G旁路紧贴 |
| XMOS BCK/WS/DATA → TDA1543 | <20mm | **三线等长** |
| TDA1543 Iout → OPA1656 反相端 | **<8mm** | 全板最关键走线 |
| OPA1656 → LPF | <10mm | - |
| LPF → RCA | <15mm | 含隔直+阻尼 |

**I2S走线处理：**

XMOS到四颗TDA1543的I2S是共享总线（四颗芯片接收相同信号）：

```
XMOS BCK  ──[Rs_bck]──┬──▶ TDA1543 #1 BCK
                        ├──▶ TDA1543 #2 BCK
                        ├──▶ TDA1543 #3 BCK
                        └──▶ TDA1543 #4 BCK

XMOS WS   ──┬──▶ #1 WS ──▶ #2 WS ──▶ #3 WS ──▶ #4 WS
              （或星形扇出）

XMOS DATA ──┬──▶ #1 DATA ──▶ #2 DATA ──▶ #3 DATA ──▶ #4 DATA

Rs_bck：BCK串联电阻预留位（0603，默认0Ω）
  BCK是三根线中边沿最敏感的（锁存时序参考）
  4颗芯片的负载电容增大（~4×5pF=20pF），可能需要更关注边沿质量
  如果示波器观察到BCK边沿振铃或过冲：装22~33Ω做边沿整形

走线策略（菊花链或短星形）：
  从XMOS出发，先到左声道组（#1→#2），再延伸到右声道组（#3→#4）
  同组两颗之间走线极短（<3mm）
  首末颗之间走线长度差 < 8mm
  （在44.1kHz BCK=1.41MHz@32fs下，8mm传输延迟~40ps，远小于时序裕量）
```

**Iout并联汇合走线——全板最关键的走线：**

```
左声道：
  TDA1543 #1 IOUTL ──┐
                      ├── 汇合点 ── <5mm ── OPA1656(L) 反相输入
  TDA1543 #2 IOUTL ──┘

右声道：
  TDA1543 #3 IOUTR ──┐
                      ├── 汇合点 ── <5mm ── OPA1656(R) 反相输入
  TDA1543 #4 IOUTR ──┘

汇合规则：
• 两颗Iout到汇合点的走线**等长**（对称星形或Y形）
  不等长会引入相位差 → 在高频段两颗电流不完全同相叠加
• 汇合点到OPA1656反相端 <5mm，尽可能宽（≥0.3mm）
• 不要在汇合点或Iout走线下方有任何数字信号走线
• 走线正下方Layer 2地平面必须连续
• 同组两颗TDA1543物理相邻（纵向排列），Iout引脚朝同一侧
```

**TDA1543 GND/VDD引脚去耦与接地——DIP-8寄生电感对策：**

```
DIP-8引脚长度~3mm → 每根引脚约2-3nH寄生电感。
R2R开关瞬态电流变化率 di/dt 在GND引脚上产生地弹（ground bounce）。
如果两颗同组TDA1543共享GND回流路径，
一颗的地弹叠加到另一颗的信号参考上 → 并联噪声消除不完美。

规则：
• 每颗TDA1543的VDD引脚各自独立去耦到Layer 2地平面
  去耦电容（100nF C0G + 10μF MLCC）到引脚走线 < 2mm
• 每颗TDA1543的GND引脚各自独立过孔到Layer 2地平面
  不在Top层走线上汇合——汇合发生在地平面铜皮上
• 过孔紧贴GND焊盘（< 1mm），单个过孔即可（0.3mm drill）
• 同理，VDD去耦电容的GND端也独立打过孔
```

**USB走线与模拟信号走线隔离：**

```
USB差分对和SOF协议活动是板上最强的周期性EMI源（1kHz/8kHz）。
XMOS数字区与模拟核心区已通过布局分区隔离。

额外规则：
• USB差分走线到模拟信号走线（I/V节点、LPF、Iout）最小间距 ≥ 10mm
• 两者之间的Layer 2地平面必须完整覆盖（不能有过孔密集区形成"漏洞"）
• USB走线尽可能走PCB边缘，远离模拟核心区中线
```

### 8.4 接地策略

```
USB Shield ── 直连 → CGND (RF屏蔽/外壳)
USB Shield ── 100Ω // 1nF → SGND (高阻隔离+RF旁路)
CGND ── 单点（电源入口M3孔）→ SGND
RCA GND = SGND (直连)

Layer 2地平面：完整铜皮，不切割，不挖槽。
数字/模拟分区通过布局实现，不通过地平面分割。
```

### 8.5 Layer 3电源平面

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│   ┌──────────────┐     ┌──────────────────────────┐    │
│   │              │     │                          │    │
│   │  3.3V_D      │     │      +7V_DAC              │    │
│   │  (XMOS)      │     │  (TDA1543×4 only)         │    │
│   │              │     │                          │    │
│   ├──────────────┤     ├──────────────────────────┤    │
│   │  3.3V_CLK   │     │      +9V_IV              │    │
│   │  (晶振,小岛) │     │  (OPA1656×2 + 偏置)     │    │
│   └──────────────┘     └──────────────────────────┘    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 8.6 去耦完整清单

| 位置 | 元件 | 类型 | 数量 | 到引脚距离 |
|------|------|------|------|-----------|
| 晶振 VCC (×2) | 100nF + 10nF | C0G | 4 | <1mm |
| XMOS VDD (多引脚) | 10μF + 100nF 每组 | MLCC+X7R | ~10 | <3mm |
| TDA1543 VDD (×4) | 10μF + 100nF + 10nF | MLCC+X7R+C0G | 12 | ≤1mm |
| OPA1656 V+ (×2) | 10μF + 100nF | MLCC+X7R | 4 | <2mm |
| 信号I/V偏置旁路 (×2) | 47μF电解 + 2.2μF PP + 100nF | 电解+PP+C0G | 6 | - |
| dummy偏置旁路 (×4) | 47μF电解 + 100nF | 电解+C0G | 8 | - |
| LDO输入/输出 (×3路) | 10μF + 100nF 每路 | MLCC+X7R | 12 | <3mm |
| **总计** | | | **~52颗** |

对比：v3.0离散R2R需要~116颗去耦电容。v3.4.2只需~56颗（含偏置旁路PP薄膜）。

---

## 9. 未用输出处理

### 9.1 每颗TDA1543的未用通道

```
左声道并联组：
  TDA1543 #1：IOUTL → 并联汇合 → I/V(L) （使用）
              IOUTR → Rf_dummy → 偏置点_1  （不使用）

  TDA1543 #2：IOUTL → 并联汇合 → I/V(L) （使用）
              IOUTR → Rf_dummy → 偏置点_2  （不使用）

右声道并联组：
  TDA1543 #3：IOUTR → 并联汇合 → I/V(R) （使用）
              IOUTL → Rf_dummy → 偏置点_3  （不使用）

  TDA1543 #4：IOUTR → 并联汇合 → I/V(R) （使用）
              IOUTL → Rf_dummy → 偏置点_4  （不使用）
```

### 9.2 未用通道的负载

```
每颗TDA1543各自独立的偏置网络（共4组）：

偏置点_N = +9V_IV经独立分压：10kΩ/10kΩ + 47μF // 100nF → 4.50V
Rf_dummy = 2.5kΩ（与信号Rf相同）

关键规则：4个偏置点互不共享。

dummy偏置电压必须与信号I/V虚地电压（4.5V）匹配，
让使用/未使用通道看到相同的Iout端电压 → 内部电流镜对称。
偏置源从+9V_IV分压（不从+7V_DAC），与信号I/V偏置同源，切断电源串扰路径。

原因同v3.3：dummy通道的电流输出是信号相关的，
如果共享偏置节点会形成跨芯片串扰路径。
并联拓扑下更关键——同组两颗芯片的dummy如果共享偏置，
它们的dummy电流会叠加并通过偏置阻抗交叉调制。

每颗芯片的dummy偏置网络元件：
  2× 10kΩ 1% 薄膜 + 1× 47μF电解 + 1× 100nF C0G + 1× 2.5kΩ Rf_dummy
  4颗芯片共需：8颗电阻 + 4颗电解 + 4颗C0G + 4颗Rf_dummy = 20颗被动元件

布局简化：
  同组两颗TDA1543的dummy偏置网络可以共享PCB区域（紧邻布局），
  但电气上必须独立（不共享走线/焊盘）。
```

---

## 10. 验证计划

### 阶段一：XMOS验证（开发板）

| 任务 | 通过标准 |
|------|----------|
| macOS integer mode | Roger Player选中Int32或Int24路径 |
| **32fs帧格式输出（44.1k/48k）** | **示波器：WS半周期内恰好16个BCK；WS翻转后1个BCK延迟才出MSB（标准I2S时序）** |
| **32fs帧格式输出（176.4k/192k）** | **示波器：确认高采样率下没有偷偷退回64fs（仍然是每半周期16个BCK）** |
| I2S 16-bit输出格式 | 示波器：BCK/WS/DATA波形正确 |
| 外部MCLK模式 | BCK/WS频率与晶振一致 |
| 晶振OE切换 | GPIO控制两颗晶振互斥使能，切换后BCK频率正确 |
| **OE切换毛刺检查** | **示波器同时看MCLK和BCK/WS，切换采样率时无异常毛刺导致单次错帧** |
| 采样率切换 (44.1~96kHz) | 无pop/click |
| 长时间稳定性 | 4小时无异常 |

**32fs是阶段一的第一优先验证项——Gate-1（见第16节）。**

如果XMOS开源固件不支持32fs：
- 评估修改固件源码的可行性（XMOS开源固件可改）
- 若不可行，回退到64fs降级方案
- **注意：回退到64fs影响所有采样率**（不只影响高采样率）——
  44.1k/48k也会变成64fs，此时需要用32fs vs 64fs对照实验（见3.6节）
  确认64fs下TDA1543的杂散/闲置音表现可接受
- 最高采样率硬限制为96kHz（64fs × 96k = 6.144MHz, 67% of BCKmax）

### 阶段二：TDA1543来料验证

| 任务 | 通过标准 |
|------|----------|
| 外观检查 | 印字清晰、封装一致、引脚无腐蚀 |
| 静态电流 | VDD=7V，I_supply与datasheet趋势一致（datasheet @5V: 50mA typ） |
| 功能测试 | 1kHz满幅I2S → Iout有信号 |
| 批次一致性 | ≥6颗芯片输出幅度差异 <5% |
| **并联配对** | **从通过一致性的芯片中，选4颗输出幅度最接近的作为一组** |

**如果来料验证失败（疑似假货）：**
- 更换供应商重新采购
- 如果无法获得可靠芯片 → 评估退回v2.1 (AD1865) 或推进v3.0 (离散R2R)

### 阶段三：I/V + 完整模拟级（面包板/万能板）

| 任务 | 通过标准 |
|------|----------|
| I/V直流工作点 | OPA1656输出在4.5V±0.5V |
| I/V稳定性 (10kHz方波) | 无振铃、无振荡 |
| I/V容性负载 (1nF) | 稳定 |
| I/V不同电池电压 (10.95/9.0/7.5V) | 全部稳定 |
| 输出电平 (1kHz满幅) | 1.94 Vrms ±15%（Rf=2.5kΩ，+9V供电） |
| **单颗 vs 2并联对比** | **同一1kHz信号：2并联输出电压 ≈ 2× 单颗（±5%偏差内）** |
| **并联idle tone测试** | **-60dBFS信号下：2并联的idle tone低于单颗（DNL平均效果）** |
| TDA1543满幅电流实测 | 记录值，修正Rf如需 |
| LPF+输出 (1m RCA到后级) | 高频响应无异常 |

**I/V偏置点+供电验证（Gate-2，见第16节）：**

```
TDA1543 VDD=+7V，偏置4.5V → 合规裕量1.3V（设计保证）。
OPA1656 +9V供电，4.5V = 精确中点 → 正负半周完美对称。
Gate-2实验目的从"判定能否使用"变为"记录THD基线数据"。

  测量项目（2并联，Rf=2.5kΩ，+9V供电）：
  □ THD @ 1kHz, 0/−6/−20/−60 dBFS
  □ THD随摆幅变化曲线
  □ Rf=2.5kΩ/2.2kΩ/2.0kΩ对照
  □ TDA1543 VDD=7V下IFS实测

  预期：4.5V/Rf=2.5kΩ直接通过 → 冻结，输出1.94Vrms
  退档（如有异常）：见5.4节电平预算

PCB预留：
  OPA1656 V+: 跳线位，可选+9V_IV或+7V_DAC
  偏置上臂：两个0603串联焊盘（可组合阻值）
  Rf: 0805焊盘，支持2.0kΩ/2.2kΩ/2.5kΩ三档
```

### 阶段四：系统集成（PCB原型）

**进入条件：Gate-1、Gate-2、Gate-3、Gate-4全部通过。原理图冻结。**

| 任务 | 通过标准 |
|------|----------|
| THD+N (1kHz, 0dBFS) | < -85 dB |
| 输出底噪 (A-wtd) | < -100 dBV |
| 线性度 @ -60dBFS | < ±1 dB |
| 线性度 @ -90dBFS | < ±2 dB |
| 通道串扰 (1kHz) | < -90 dB |
| J-test | 无异常边带；同时作为PLL对照实验（见下） |
| NOS镜像抑制 (fs=44.1k) | 四阶Bessel LPF镜像频点验收（见5.7节指标表） |
| 系统级IMD (双音15k+19k) | DAC→HEV90链路，音频带内IMD产物低于底噪+10dB（见5.7节） |
| FIFO稳定性 | 8小时无爆音 |
| I/V偏置分压噪声 | 分压网络噪声不出现在输出频谱 |
| Microphonics（轻敲测试） | 轻敲壳体/桌面，输出频谱无对应频率尖峰（见下方协议） |
| USB SOF杂散 | 输出频谱在1kHz及其谐波(2k/3k/4k)处无与音乐无关的固定尖峰 |

**J-test对照实验协议（阶段四关键验证项）：**

J-test的目的不仅是"看有没有边带"，而是通过对照实验判断XMOS内部PLL是否在I2S时钟路径上。

```
实验1：外部MCLK模式（设计配置）
  CCHD-957 → XMOS MCLK输入
  记录：J-test频谱，标注所有边带位置和幅度

实验2：不同采样率下的边带模式
  44.1k / 48k / 88.2k / 96k 各跑一次J-test
  记录：边带是否随采样率系统性变化
  如果44.1k系和48k系的边带模式不同 → 可能与晶振质量差异有关
  如果边带模式相同 → 更可能是XMOS内部处理引入

实验3（如XMOS固件支持）：内部MCLK模式
  关闭外部MCLK，使用XMOS内部PLL生成时钟
  记录：J-test频谱
  对比实验1：如果边带幅度/模式显著不同 → PLL路径可区分

结论判定：
  实验1边带 << 实验3边带 → 外部MCLK有效绕过了PLL
  实验1边带 ≈ 实验3边带 → PLL可能仍在路径上
  （无论结论如何，记录数据，不做过度解读）
```

**Microphonics测试协议（阶段四验证项）：**

```
MLCC压电效应 + 薄铝壳谐振 → PCB振动 → 信号路径电容产生微电压噪声。
HE90开放式外泄声压→桌面→壳体的路径是真实的。

测试方法：
  □ DAC静音输出（无音乐信号），频谱分析器接RCA输出
  □ 用指关节轻敲壳体顶盖/底壳/侧面，观察频谱变化
  □ 用铅笔尾端轻敲桌面（模拟正常使用中的桌面振动）
  □ 记录是否出现与敲击对应的频谱尖峰及其幅度

验收标准：
  □ 轻敲产生的尖峰幅度 < 底噪 + 6dB（不显著高于背景噪声）
  □ 如不通过：
    - 检查LPF电容是否为PP薄膜（不是MLCC）
    - 检查PCB橡胶垫圈是否到位
    - 考虑在壳体内加阻尼材料（薄层丁基橡胶贴片）
```

**USB SOF杂散测试协议（阶段四验证项）：**

```
USB协议每1ms发送SOF包（Full Speed 1kHz，High Speed 125μs/8kHz）。
周期性数字活动可能通过辐射或电源耦合到模拟域。

测试方法：
  □ DAC播放静音文件（数字零），频谱分析器观察输出
  □ 重点检查1kHz、2kHz、3kHz...处是否有固定窄带尖峰
  □ 对比：USB连接播放静音 vs DAC输入端断开（如可能）
  □ 如果出现1kHz尖峰，测量其幅度

验收标准：
  □ 1kHz及其谐波处，任何尖峰幅度 < -110dBV（低于A-wtd底噪指标）
  □ 如不通过：
    - 检查USB走线到模拟走线距离（≥10mm？）
    - 检查USB CMC是否正确安装
    - 在XMOS VDD输入端加小电感（铁氧体磁珠+电容组成π滤波）
```

### 阶段五：端到端

| 任务 | 通过标准 |
|------|----------|
| Roger Player → Roger DAC全链路 | 与阶段四一致 |
| 采样率覆盖 (44.1/48/88.2/96kHz) | 全部正常 |
| 176.4k/192kHz（可选） | 如验证通过则启用，否则标注不支持 |
| 位深 (16-bit PCM) | bit-perfect |
| 电池续航 | 记录时间（预期4~5h），红灯在~9.0V亮起 |
| 三档拨杆 | OFF/ON/CHARGE各档位功能正确，ON↔CHARGE切换无电气串通 |
| 充电指示 | CHARGE模式：充电中慢闪红，充满慢闪绿 |

---

## 11. 已知妥协

| 妥协 | 原因 | 影响 | 态度 |
|------|------|------|------|
| 16-bit | TDA1543规格 | CD无损，24-bit截断在Roger Player层 | 接受 |
| 串行输入（非并行） | IC架构 | 内部串行移位，不在PCB上 | 接受 |
| I/V转换（额外有源环节） | 电流输出DAC | 成熟电路，风险低 | 接受 |
| 输出~1.94Vrms | 2并联+Rf=2.5kΩ，+9V独立供电 | 接近标准线电平2Vrms；大多数耳放/功放可直接驱动 | Rf三档可调+供电回退 |
| NOS镜像通过 | 设计意图 | 超声能量 | 设计意图 |
| 176.4k/192kHz待验证 | 32fs结构可支持，但TDA1543高频表现未确认 | 默认支持至96kHz | 验证后启用 |
| 停产风险 | TDA1543已停产 | 需备货（并联方案需4颗/台），长期需替代方案 | 备足+规划v3.0 |

---

## 12. 全版本对比

| 维度 | v2.1 AD1865 | v3.0 离散R2R | **v3.4.2 TDA1543×4** |
|------|-------------|-------------|---------------------|
| 信号路径环节 | 6 | 5 | **4** |
| 格式适配 | CPLD（必须） | FPGA（必须） | **无** |
| 负电源 | 电荷泵+LC+LDO | 无 | **无** |
| I/V转换 | 需要 | 不需要 | 需要（独立+9V供电） |
| LDO数量 | 4+负LDO | 5+Vref | **4**（2×LT3042 + 2×LT1763） |
| 主要IC | 4 (XMOS+CPLD+AD1865×2) | 35 (XMOS+FPGA+32×开关) | **5 (XMOS+TDA1543×4)** |
| 去耦电容 | ~60 | ~116 | **~54** |
| I2S帧格式 | N/A（非I2S） | N/A（并行） | **32fs（语义完全）** |
| PCB面积 | ~100×70mm | ~120×80mm | **~90×60mm** |
| 有效位宽 | ~16 bit | 14~16 bit | 16 bit（+3dB SNR via并联） |
| 输出电压 | ~2.0Vrms | ~2.0Vrms | **~1.94Vrms**（+9V供电） |
| 等效动态范围 | ~96dB | ~88dB | **~90dB**（并联提升） |
| 最高采样率 | 192k(待验) | 96k(稳)/192k(待验) | **192k（结构可支持，验证后启用）** |
| 工程风险 | 中 | 高 | **低** |
| 单台成本 | ~$327 | ~$389 | **~$222** |
| 供货风险 | 低(在产) | 无(电阻) | **高(停产，需4颗/台)** |

---

## 13. 元器件清单

### 主要器件

| 类别 | 型号 | 数量 | 估价 |
|------|------|------|------|
| USB+FIFO | XMOS XU216-512-TQ128 | 1 | $15 |
| XMOS开发板（验证） | xCORE-200 MC Audio | 1 | $100 |
| DAC | TDA1543 DIP-8 | 4+备4 | $40 |
| I/V运放 | OPA1656 SOIC-8 | 2 | $7 |
| 晶振 | Crystek CCHD-957 22.5792MHz | 1 | $30 |
| 晶振 | Crystek CCHD-957 24.576MHz | 1 | $30 |

### 电源器件

| 类别 | 型号 | 数量 | 估价 |
|------|------|------|------|
| 电池 | LiFePO4 18650 3S | 3 | $12 |
| BMS | 3S保护板 | 1 | $5 |
| 充电器 | 3S CC/CV（外部） | 1 | $15 |
| LDO (时钟) | LT3042 | 1 | $5 |
| LDO (DAC +7V) | LT1763 | 1 | $3 |
| LDO (I/V +9V) | LT3042 | 1 | $5 |
| LDO (数字3.3V) | LT1763 | 1 | $3 |
| DP3T拨杆 | OFF/ON/CHARGE三档 | 1 | $3 |
| 电源管理 | LM393+双色LED+DC jack+RC振荡+分压 | 1组 | $2 |

### 被动元件

| 类别 | 规格 | 数量 | 估价 |
|------|------|------|------|
| Rf (I/V) | 2.5kΩ 0.1% **薄膜** 0805 TCR≤25ppm/°C（备2.0kΩ/2.2kΩ） | 2+备 | $3 |
| Rf_dummy | 2.5kΩ 1% 薄膜 0805 | 4 | $0.4 |
| Cf | 10pF + 可调位 | 4 | $0.5 |
| Rs预留 | 0Ω/10Ω/33Ω 0603 | 2+备 | $0.3 |
| 偏置分压（信号I/V） | 10kΩ+10kΩ 1% 薄膜 0603 /声道 | 4 | $0.3 |
| 偏置分压（dummy） | 10kΩ+10kΩ 1% 薄膜 0603 /芯片 | 8 | $0.6 |
| 偏置旁路（信号I/V） | 47μF电解 + **2.2μF PP薄膜** + 100nF C0G ×2 | 6 | $3 |
| 偏置旁路（dummy） | 47μF电解 + 100nF C0G ×4 | 8 | $4 |
| 晶振OE下拉 | 10kΩ 0402 | 2 | $0.1 |
| I2S BCK串联预留 | 0Ω/22Ω/33Ω 0603 | 1+备 | $0.1 |
| 晶振串联 | 22Ω 0402 | 2 | $0.1 |
| LPF R+C | 四阶Bessel：R=薄膜，C=**聚丙烯薄膜优先**（体积不允许则C0G），按仿真值 | 16 | $3 |
| 隔直 | 10μF **聚丙烯（PP）薄膜** | 2 | $4 |
| 输出 | 47Ω + 100pF | 4 | $0.5 |
| 去耦电容 | 各规格（见8.6） | ~54 | $8 |
| 磁珠 | 1kΩ@100M / 600Ω@100M | 3 | $0.6 |
| ESD | USBLC6-2 + CMC | 2 | $1 |
| **被动小计** | | | **~$27** |

### 成本概算

| 类别 | 金额 |
|------|------|
| 主要器件 | ~$122 |
| XMOS开发板 | ~$100 |
| 电源器件 | ~$53 |
| 被动元件 | ~$27 |
| 结构件 | ~$20 |
| **总计（含开发板）** | **~$322** |
| **量产单台** | **~$222** |

---

## 14. 端到端信号路径

```
Roger Player (macOS)
  │  symphonia解码 → i32左对齐 (16-bit有效)
  │  Lock-free Ring Buffer
  │  HALOutput + Physical Format + Hog Mode
  ▼
USB Audio Class 2.0 (异步, integer PCM, 16-bit)
  ▼
XMOS XU216
  │  内部异步FIFO
  │  速率反馈端点（不丢弃样本）
  │  GPIO控制晶振OE（44.1k系/48k系互斥）
  │  本地晶振分频 → BCK/WS/DATA（32fs帧格式）
  ▼
I2S 直连（3线广播：BCK, WS, DATA → 4颗TDA1543）
  │  32fs：每声道16个BCK，每个BCK承载有效数据位
  │  无CPLD，无FPGA，无格式适配，无填充时钟
  ▼
TDA1543 × 4（2并联/声道，双单声道）
  │  I2S接收 → 内部移位寄存器 → R2R梯形网络 → 电流输出
  │  每声道2颗Iout并联：电流×2，噪声×√2，SNR +3dB
  │  16-bit，无内部数字滤波（NOS）
  ▼
OPA1656 I/V（独立+9V供电，摆幅0.25V~8.75V @2kΩ负载）
  │  Rf=2.5kΩ → ±2.75V around 4.5V DC → ~1.94Vrms
  │  电流→电压，无增益/无衰减
  ▼
四阶Bessel LPF (fc≈70kHz，两段二阶section级联，验收标准见5.7节)
  │  衰减超声镜像（@44.1kHz ≥24dB）
  │  同阶最平坦群延迟，最小振铃倾向
  │  纯无源，8个被动元件（4R+4C）
  ▼
10μF PP薄膜 (去除4.5V直流偏置，DA≤0.05%) → 47Ω + 100pF → RCA (~1.94 Vrms)
```

全程：
- 无float转换
- 无数字滤波
- 无升采样
- 无PLL
- 无格式适配逻辑
- 无负电源
- 信号路径上的有源器件：XMOS（数字）+ TDA1543×2并联（DAC）+ OPA1656（I/V）= 3类/5个

时钟链路：
晶振 → XMOS分频 → BCK/WS → TDA1543
从晶振到DAC转换事件，只经过数字分频。

---

## 15. 进化路径

| 方向 | 方法 | 对v3.4.2架构的影响 |
|------|------|------------------|
| 进一步提高SNR | 4并联/声道（共8颗TDA1543） | 中——需扩板到100×80mm，+3dB SNR（90→93dB），输出电压不变（被compliance锁死） |
| 平衡输出 | 每声道增加一颗OPA1656反相级 | 小——+2颗运放，差分输出3.88Vrms |
| 启用176.4/192kHz | 验证TDA1543高频表现，启用XMOS 32fs高采样率配置 | 零——32fs已为此预留结构余量 |
| 替代停产TDA1543 | 迁移到v3.0离散R2R | 中——重做DAC核心，复用电源/XMOS/布局经验 |

v3.4.2是v3.0的工程落地踏板。
在v3.4.2上验证完整链路后，如果要追求终极形态，可以把DAC核心换成v3.0离散R2R，
其余部分（XMOS前端、电源架构、PCB布局经验、I/V调试经验）全部复用。

---

## 16. Gate清单（不通过不进下一阶段）

工程闸门。每个Gate在对应阶段结束前必须通过。不通过则执行对应的降级/修改方案。

### Gate-1：32fs帧格式确认（阶段一结束前）

```
条件：示波器确认XMOS输出真32fs
  □ 44.1k/48k：WS半周期内恰好16个BCK
  □ WS翻转后1个BCK延迟出MSB（标准I2S时序）
  □ 176.4k/192k：同样16个BCK/半周期（不退回64fs）

通过 → 进入阶段二，32fs作为最终帧格式冻结
不通过 → 文档自动切换到"64fs降级方案"：
  □ 所有采样率（含44.1k/48k）均为64fs
  □ 最高采样率硬限制96kHz
  □ 必须执行32fs vs 64fs对照实验（见3.6节），确认64fs下无异常杂散
  □ 同步更新文档所有BCK频率表、采样率支持策略
```

### Gate-2：I/V偏置点+Rf冻结（阶段三结束前）

```
前提：VDD=+7V / 偏置4.5V / +9V供电 → 合规裕量1.3V + OPA对称
      合规性由设计保证，Gate-2仅冻结最终Rf值。

条件：
  □ THD @ 0/−6/−20/−60 dBFS 四档基线数据
  □ Rf=2.5kΩ/2.2kΩ/2.0kΩ THD对照
  □ TDA1543 VDD=7V下IFS实测

通过路径（按预期顺序）：
  A: 4.5V + Rf=2.5kΩ THD正常 → 冻结，1.94Vrms（预期路径）
  B: Rf=2.2kΩ更优 → 降档，1.71Vrms
  C: 偏置4.0V更优 → 改分压(12.7kΩ/10kΩ)，1.94Vrms
  D: +9V不理想 → 跳线切+7V_DAC，偏置4.3V，Rf=2.2kΩ，1.71Vrms
  E: 全部异常 → 换芯片重测（排除个体问题）
```

### Gate-3：时钟OE切换无毛刺（阶段一结束前）

```
条件：切换采样率时MCLK/BCK/WS干净
  □ 示波器同时捕获MCLK和BCK/WS
  □ 切换瞬间无双驱动毛刺
  □ 三态悬浮期间MCLK无耦合尖峰导致错帧
  □ 上电初始化过程中两颗晶振均为关闭态

通过 → OE直控方案确认，无需外部MUX
不通过 → 按以下顺序尝试修复：
  1. 增大禁用→使能的间隔（1μs → 10μs → 100μs）
  2. 检查10kΩ下拉是否足够快速放电三态线
  3. 如仍不可接受 → 增加最小MUX缓冲（单颗74LVC1G157或等效）
```

### Gate-4：热设计验证（阶段三结束前）

```
条件：密封铝壳内全部热源在最坏条件下可控
  □ 电池满充（10.95V）+ 连续运行2小时
  
  LT1763 ×2验证：
  □ LT1763 #2（+7V_DAC，0.82W）壳温 < 100°C
  □ LT1763（3.3V_D，0.92W）壳温 < 100°C
  □ 两颗LT1763不触发热保护关断
  □ 两颗LT1763布局互相远离，热场不叠加
  
  TDA1543 DIP-8验证：
  □ TDA1543壳温实测（DIP-8顶部热电偶）
  □ 4颗中最热一颗的壳温 < 75°C（对应结温~85°C，含10K/W壳-结差）
  □ GND引脚散热路径有效（热过孔导通到底层铜皮→导热垫→铝壳）
  
  系统级：
  □ 模拟区（OPA1656附近）温升 < 5°C（相对环境温度）
  □ 壳体外表面温度 < 45°C（舒适触摸上限）

通过 → 当前热设计确认
不通过 → 按热源定位修复：
  LT1763(3.3V)过热 → 启用预降压方案D1（TPS62160降压到~5V再LDO）
  LT1763(+7V)过热 → 加大铜皮面积 / 增加散热过孔
  TDA1543过热 → 加大GND引脚铜皮 / 增加底层散热过孔阵列
  系统级过热 → 评估壳体通风孔（侧面冲孔，小面积，不影响EMI）
```

### Gate通过记录

| Gate | 阶段 | 结果 | 日期 | 备注 |
|------|------|------|------|------|
| Gate-1 32fs | 阶段一 | 待定 | - | - |
| Gate-2 偏置+Rf+供电 | 阶段三 | 待定 | - | - |
| Gate-3 OE切换 | 阶段一 | 待定 | - | - |
| Gate-4 温升 | 阶段三 | 待定 | - | - |

**规则：Gate-1和Gate-3必须全部通过后，才能进入PCB设计（阶段四）。
Gate-2和Gate-4必须全部通过后，才能冻结原理图发给板厂。**

---

## 附录：修订历史

| 版本 | 核心变更 |
|------|---------|
| v1.0~v1.2 | Amanero + FPGA + AD1865 |
| v2.0 | XMOS替代Amanero+FPGA |
| v2.1 | 修正AD1865接口，加CPLD |
| v3.0 | 第一性原理：离散并行R2R |
| v3.1 | TDA1543落地：原生I2S + 单+5V，消除CPLD/FPGA/负电源，4环节信号链 |
| v3.2 | 工程审校：32fs帧格式、OE晶振切换、OPA1656 headroom修正、CCHD-957电流修正、dummy偏置隔离、Rs工程定义、LPF验收标准、J-test对照实验 |
| **v3.3** | **出板前评审：Gate闸门机制（32fs/偏置点/OE毛刺/温升）、I/V compliance voltage验证、32fs vs 64fs对照实验、OE切换安全时序、LT1763散热必做+预降压方案、BCK串联电阻预留** |
| **v3.4** | **2并联TDA1543增强动态：每声道2颗并联（共4颗），SNR +3dB（~90dB）、DNL ε/√2、输出1.17Vrms（Rf=1.5kΩ）；Rf三档可调策略；Iout星形汇合走线规范；并联配对验证；Gate-2扩展为偏置+Rf联合冻结** |
| **v3.4.1** | **I/V独立+9V供电+四阶Bessel LPF+薄铝壳：OPA1656从+5V_A分离到独立LT3042 #3（+9V_IV），偏置3.5V，Rf=2.5kΩ薄膜→输出1.94Vrms；DAC↔I/V电源隔离；LPF从二阶升至四阶Bessel（两段二阶section级联，8被动元件/声道），镜像@44.1kHz≥24dB抑制——针对HEV90管机后级的IMD风险；电池从26650改为18650（双组×3保留DPDT热切换）；0.8mm 5052铝折弯壳140×70×30mm堆叠布局；信号路径元件材质规范（Rf薄膜、LPF电容PP优先、隔直PP DA≤0.05%、禁用X7R/X5R）；PCB橡胶垫圈防microphonics；TDA1543 GND独立过孔防ground bounce；USB走线到模拟走线≥10mm；验收新增轻敲测试+USB SOF杂散检查** |
| **v3.4.2** | **衰减自然度+TDA1543供电+偏置+电源管理全面重设计：信号I/V偏置旁路从二级升为三级（+2.2μF PP薄膜），抑制电解DA释放对衰减段偏置点的扰动；TDA1543 VDD从+5V提升到+7V（LT3042→LT1763，208mA负载），Iout合规裕量从0.3V增至1.3V；偏置从3.5V改为4.5V（+9V精确中点，10kΩ/10kΩ天然匹配），OPA1656正负半周完美对称；电池回归单组18650×3，双组DPDT替换为三档DP3T拨杆（OFF/ON/CHARGE上下排列），物理强制不边充边放；续航修正为4~5小时（TDA1543 IDD=50mA/颗纳入计算）；量产单台$222** |

---

*文档版本: 3.4.2*
*日期: 2026-02*
*配套软件: Roger Player*
