# Roger DAC v3.4.2 — 交付 PCB 前验证 SOP

> 范围：从当前状态到原理图冻结、交付板厂之前，所有必须完成的验证、修改、确认步骤。
> 前提文档：`roger_dac_v3.4.2.md`（设计文档）、`preparation_summary.md`（固件前置准备）。
> 硬性规则：**Gate-1 + Gate-3 全部通过 → 才能开始 PCB 布局；Gate-2 + Gate-4 全部通过 → 才能冻结原理图发板厂。**

---

## 阶段〇：工具链与编译（当前阻塞项）

### S0.1 安装 XTC Tools 15.3

```
□ 从 https://www.xmos.com/software-tools 下载 XTC Tools 15.3.0
□ 安装完成后执行：source ~/XMOS/XTC/15.3.0/SetEnv
□ 验证：xcc --version 输出包含 15.3
□ 验证：xmake --version 正常返回
```

### S0.2 编译固件

```
□ cd sw_usb_audio/app_roger_dac
□ cmake -G "Unix Makefiles" -B build
  — 观察 CMake 输出：lib_xua 是否成功拉取（develop 分支）
  — 确认无 "XUA_I2S_N_BITS undeclared" 或类似警告
□ xmake -j -C build
□ 编译产物确认：bin/ 下存在 roger_dac.xe
```

### S0.3 源码确认（编译后、烧录前，10 分钟）

这一步的目的：**在接示波器之前，从源码层面确认 32fs 一定会生效，避免白跑。**

```
□ 在 lib_xua 源码中 grep "XUA_I2S_N_BITS"，确认它出现在：
  — lib_xua/src/core/audiohub/xua_audiohub.xc 的 numBits 计算行
  — 公式应为：numBits = XUA_I2S_N_BITS * I2S_CHANS_PER_FRAME
  — 如果 grep 不到 → 说明 develop 分支版本不对，需要检查 deps.cmake 拉取的 commit

□ 确认 lib_xua 中没有硬编码的 64fs fallback 逻辑：
  — grep "64" 或 "32" 在 xua_audiohub.xc 附近，看是否有 
    "if numBits < 64 then numBits = 64" 之类的保护代码
  — 如果有 → 需要评估是否可以安全绕过或修改

□ 确认设计文档 §4.3 标注的 MCLK_BCK_RATIO（???）：
  — 在 xua_conf.h 和 lib_xua 中搜索 MCLK_BCK_RATIO 或类似宏
  — 如果 lib_xua 自动从 XUA_I2S_N_BITS 推导 → 不需要手动设
  — 如果需要手动设 → 补充到 xua_conf.h 并重新编译
```

**判定：S0.3 全部通过 → 进入 S1（烧录+示波器验证）。**
**如果发现 lib_xua 不支持 XUA_I2S_N_BITS=16 → 停止，评估源码修改可行性或回退 64fs 降级方案。**

---

## 阶段一：XMOS 开发板验证（Gate-1 + Gate-3）

### 所需设备

```
□ XK-AUDIO-216-MC 开发板（已有）
□ XTAG 调试器（随开发板）
□ 示波器（≥2 通道，带宽 ≥ 20MHz，采样率 ≥ 50MSa/s）
  — 6.144MHz BCK（192k 下）需要至少 5 倍过采样才能可靠计数
□ 示波器探头 ×2（标配 10:1 即可）
□ macOS 电脑（运行 Roger Player 或任意音频播放器）
□ USB-A/C 线缆（连接 Mac 到开发板）
```

### S1.1 烧录与基本连接

```
□ xrun --io bin/roger_dac.xe
□ Mac 上确认 USB 音频设备出现（系统偏好设置 → 声音 → 输出）
  — 设备名应包含 "XMOS" 或自定义名称
  — 如果不出现：检查 AUDIO_CLASS=2 是否生效、macOS UAC2 驱动是否加载
□ 设置输出格式为 16-bit / 44100Hz（音频 MIDI 设置.app）
□ 播放任意音频，确认开发板 I2S 引脚上有信号活动
  — 用示波器粗看 BCK 引脚：应有连续方波
  — 用示波器粗看 WS 引脚：应有与采样率对应的方波（44.1kHz → ~44.1kHz）
```

### S1.2 Gate-1 验证：32fs 帧格式确认 ★

**这是整个 SOP 中最关键的单点验证。不通过则全局降级。**

#### S1.2.1 测量准备

```
□ 示波器 CH1 → WS 引脚
□ 示波器 CH2 → BCK 引脚
□ 触发源：CH1（WS），触发模式：边沿触发，上升沿
□ 时基：调到能看清一个完整 WS 周期内的所有 BCK 脉冲
  — 44.1kHz 时：WS 周期 ≈ 22.7μs，半周期 ≈ 11.3μs
  — 建议时基 2~5μs/div
□ 确保 BCK 波形清晰可数（如果边沿模糊，降低探头补偿电容或换探头）
```

#### S1.2.2 逐采样率验证

**每个采样率都要独立验证，不能假设"44.1k 通过则其他也通过"。**

| 步骤 | 操作 | 通过条件 | 记录 |
|------|------|----------|------|
| A | Mac 设为 44100Hz，播放音频 | WS 半周期内恰好 **16 个** BCK | □ 通过 / □ 不通过（实测 \_\_ 个） |
| B | Mac 设为 48000Hz | 同上，16 个 BCK | □ 通过 / □ 不通过（实测 \_\_ 个） |
| C | Mac 设为 88200Hz | 同上，16 个 BCK | □ 通过 / □ 不通过（实测 \_\_ 个） |
| D | Mac 设为 96000Hz | 同上，16 个 BCK | □ 通过 / □ 不通过（实测 \_\_ 个） |
| E | Mac 设为 176400Hz | 同上，16 个 BCK（确认没有退回 64fs） | □ 通过 / □ 不通过（实测 \_\_ 个） |
| F | Mac 设为 192000Hz | 同上，16 个 BCK | □ 通过 / □ 不通过（实测 \_\_ 个） |

#### S1.2.3 I2S 时序确认

```
□ 在任意采样率下（建议 44.1kHz，波形最慢最易观察）：
  — CH1 = WS, CH2 = DATA
  — 确认：WS 翻转后，DATA 上的 MSB 延迟 1 个 BCK 才出现（标准 I2S 时序）
  — 这一步用于排除 left-justified 等非标格式
  — 如果 MSB 在 WS 翻转的同一个 BCK 就出现 → 不是标准 I2S，需要检查固件 I2S_MODE 配置
```

#### S1.2.4 BCK 频率交叉验证

```
□ 用示波器测量模式读 BCK 频率，与理论值对比：

  | 采样率 | 预期 BCK | 实测 BCK | 偏差 |
  |--------|----------|----------|------|
  | 44.1k  | 1.4112 MHz | ______ MHz | ___% |
  | 48k    | 1.536 MHz  | ______ MHz | ___% |
  | 96k    | 3.072 MHz  | ______ MHz | ___% |
  | 192k   | 6.144 MHz  | ______ MHz | ___% |

  — 偏差应 < 0.1%（晶振精度范围内）
  — 如果偏差 > 1% → 检查 MCLK 源是否正确（开发板 CS2100 配置）
```

#### S1.2.5 Gate-1 判定

```
情况 A：全部 6 个采样率均为 16 BCK/半周期
  → Gate-1 通过 ✓
  → 32fs 作为最终帧格式冻结
  → 进入 S1.3（Gate-3 验证）

情况 B：44.1k~96k 通过，176.4k/192k 回退到 64fs
  → Gate-1 部分通过
  → 降级决策：最高采样率限制为 96kHz
  → 仍需执行 32fs vs 64fs 对照实验（见 S1.2.6）确认低采样率下 32fs 无问题
  → 文档标注 176.4k/192k 不支持，更新所有 BCK 频率表

情况 C：全部采样率都是 32 BCK/半周期（64fs）
  → Gate-1 不通过 ✗
  → 执行降级流程：
    1. 检查 xua_conf.h 的 XUA_I2S_N_BITS 是否真的被编译器吃到（预处理输出确认）
    2. 如果宏正确但 lib_xua 忽略 → 评估修改 lib_xua 源码
    3. 如果无法修改 → 全面切换到 64fs 降级方案
    4. 执行 S1.2.6 对照实验
    5. 最高采样率硬限制 96kHz
    6. 更新设计文档所有相关章节
```

#### S1.2.6 32fs vs 64fs 对照实验（仅在降级时执行）

```
□ 固件分别编译 32fs 和 64fs 两个版本（如果 32fs 部分可用）
□ 信号源：1kHz -60dBFS 正弦波（16-bit PCM）
□ 分别在两种帧格式下播放，示波器观察 DATA 线波形
□ 如果后续已接 DAC（阶段三面包板），则同时记录：
  — 输出频谱中是否有额外杂散/闲置音
  — 噪声地板差异
□ 记录数据，无论结论如何
```

### S1.3 Gate-3 验证：晶振 OE 切换无毛刺

**开发板阶段的 Gate-3 验证范围有限——开发板用的是 CS2100 PLL 生成 MCLK，没有两颗独立 CCHD-957。** 以下验证分两部分：开发板上能做的（S1.3.1）和必须留到最终 PCB 的（S1.3.2 标注）。

#### S1.3.1 开发板可验证项

```
□ 确认采样率切换功能正常：
  — 从 44.1kHz 切到 48kHz，再切回
  — 从 44.1kHz 切到 96kHz
  — 每次切换后 BCK/WS 频率正确

□ 确认切换过程无 pop/click：
  — 播放音乐 → 切换采样率 → 听是否有爆音
  — 如果有 → 检查固件中静音→切换→恢复的时序

□ 确认固件 GPIO 控制逻辑（代码审查，非硬件测量）：
  — audiohw.xc 中 OE 切换的 7 步时序是否完整实现
  — 步骤 2→4 之间的 ≥1μs 延迟是否有代码保证
  — 上电时 GPIO 初始化为低电平/输入模式

□ 确认 ROGER_DAC_OSC_OE_ENABLE=0 时代码路径正确：
  — 不执行 GPIO OE 操作，走开发板 CS2100 路径
  — 切换采样率时通过 CS2100 I2C 重配而非 GPIO 切换
```

#### S1.3.2 最终 PCB 必须补做的验证（此处仅记录，不执行）

```
以下项目在拿到带 CCHD-957 的最终 PCB 后执行：
  ○ 示波器同时捕获 MCLK 和 BCK/WS
  ○ 切换采样率时：
    — MCLK 无双驱动毛刺（两颗同时输出）
    — 三态悬浮期间 MCLK 无耦合尖峰
    — BCK/WS 在切换完成后干净建立
  ○ 上电过程：两颗晶振均保持关闭态直到固件初始化完成
  ○ 如不通过 → 按设计文档 §16 Gate-3 修复路径处理
```

#### S1.3.3 Gate-3 阶段一判定

```
□ 开发板可验证项全部通过 → Gate-3 阶段一通过（有条件）
  — 记录：Gate-3 在最终 PCB 上需要补充验证 OE 硬件时序
□ 开发板切换出现 pop/click 或 BCK 频率异常 → 固件问题，需要修复后重测
```

### S1.4 补充验证项

```
□ macOS integer mode 确认：
  — Roger Player 或 Audio MIDI 设置中确认 Int32 或 Int24 路径被选中
  — 如果只有 Float32 → 检查固件 AUDIO_CLASS / USB descriptor 配置

□ 长时间稳定性（可与其他工作并行）：
  — 循环播放音频 4 小时
  — 期间不操作
  — 结束后确认：BCK/WS 仍在、无 USB 断连、无异常

□ 采样率覆盖记录：
  | 采样率 | Mac 能选到 | BCK/WS 正常 | 长稳测试 |
  |--------|-----------|------------|---------|
  | 44100  | □         | □          | □       |
  | 48000  | □         | □          | □       |
  | 88200  | □         | □          | □       |
  | 96000  | □         | □          | □       |
  | 176400 | □         | □          | □       |
  | 192000 | □         | □          | □       |
```

### 阶段一交付物

```
□ Gate-1 记录表（通过/不通过 + 实测数据）
□ Gate-3 阶段一记录表
□ 编译成功的固件 roger_dac.xe
□ 源码确认记录（XUA_I2S_N_BITS 生效路径）
□ 如有降级 → 更新后的设计文档（标注 64fs + 采样率限制）
```

**阶段一判定：Gate-1 通过 + Gate-3 阶段一通过 → 进入阶段二。**

---

## 阶段二：TDA1543 来料验证

### 所需设备与材料

```
□ TDA1543 DIP-8 芯片（≥12 颗，含筛选损耗余量）
□ DIP-8 IC 座 ×2（方便换芯片测试）
□ 稳压电源（可输出 +7.0V，限流 200mA+）
□ 万用表（测电流精度 ≤ 1mA）
□ 示波器（同阶段一）
□ 面包板或万能板
□ XMOS 开发板（阶段一已验证，提供 I2S 信号源）
□ 跳线、去耦电容（100nF 陶瓷 ×4、10μF 电解 ×2）
```

### S2.1 外观检查

```
对每颗芯片逐一检查：
□ 印字清晰（"TDA1543" 或 "TDA1543A" + 批次码）
□ 同批次印字风格一致（字体、位置、深浅）
□ 封装完整无裂痕
□ 引脚无腐蚀、无弯曲、镀层均匀
□ DIP-8 引脚间距标准（2.54mm）
□ 淘汰标准：印字模糊/不一致、引脚异常 → 直接排除，不进入电气测试
```

### S2.2 静态电流测试

```
接线：
  VDD（Pin 8）← 稳压电源 +7.0V（经去耦 100nF // 10μF）
  GND（Pin 4 + Pin 5）→ 稳压电源 GND
  所有信号引脚（Pin 1/2/3）→ 悬空或接 GND
  输出引脚（Pin 6/7）→ 悬空

□ 测量 VDD 电流（串联万用表）
□ 预期值：~50-55mA（datasheet @5V 为 50mA typ，+7V 下略高）

  | 芯片编号 | IDD (mA) | 判定 |
  |----------|----------|------|
  | #01      |          | □ 通过 / □ 异常 |
  | #02      |          | □ 通过 / □ 异常 |
  | ...      |          |      |

□ 异常判定：IDD < 30mA 或 > 80mA → 排除
□ 全部芯片 IDD 应在均值 ±20% 以内
□ 如果整批 IDD 远离 50mA 预期（如全部 < 20mA）→ 高度怀疑假货，停止，换批次
```

### S2.3 功能测试

```
接线：
  VDD = +7.0V（去耦同上）
  GND = GND
  BCK（Pin 1）← XMOS 开发板 BCK 输出
  WS（Pin 2）← XMOS 开发板 WS 输出
  DATA（Pin 3）← XMOS 开发板 DATA 输出
  IOUTL（Pin 7）→ 2.5kΩ 电阻到 +3.5V 偏置点（临时偏置，仅用于看波形）
  IOUTR（Pin 6）→ 同上

□ XMOS 播放 1kHz 0dBFS 正弦波 @ 44.1kHz
□ 示波器看 IOUTL / IOUTR 上的电压波形
□ 预期：可见清晰的阶梯状正弦波形（R2R 电流输出经电阻转换的电压）
□ 如果看到纯直流或纯噪声 → 芯片无功能，排除

  | 芯片编号 | L 通道波形 | R 通道波形 | 判定 |
  |----------|-----------|-----------|------|
  | #01      | □ 正常    | □ 正常    | □ 通过 |
  | #02      | □ 正常    | □ 正常    | □ 通过 |
  | ...      |           |           |      |
```

### S2.4 批次一致性 + 并联配对

```
□ 对所有通过功能测试的芯片，在相同条件下测量：
  — 1kHz 0dBFS → IOUTL 端的峰峰电压值（经 2.5kΩ）
  — 记录左右通道各自的幅度

  | 芯片编号 | Vpp_L (mV) | Vpp_R (mV) | 与均值偏差 |
  |----------|-----------|-----------|-----------|
  | #01      |           |           |     %     |
  | #02      |           |           |     %     |
  | ...      |           |           |           |

□ 一致性标准：同批芯片输出幅度差异 < 5%
□ 并联配对：从通过一致性的芯片中，选 4 颗输出幅度最接近的作为一组
  — 左声道组：#__ 和 #__（两颗的 IOUTL 幅度最接近）
  — 右声道组：#__ 和 #__（两颗的 IOUTR 幅度最接近）
□ 标记配对结果，保留备用芯片
```

### S2.5 来料验证判定

```
情况 A：≥4 颗通过全部测试且配对成功
  → 阶段二通过 ✓
  → 标记配对芯片组，进入阶段三

情况 B：通过的芯片不足 4 颗
  → 评估：是个体问题还是批次问题
  → 如果是批次问题（如整批 IDD 异常）→ 更换供应商重新采购
  → 如果个体问题 → 补货后重测

情况 C：整批疑似假货
  → 停止。评估退路：v2.1 (AD1865) 或 v3.0 (离散 R2R)
```

### 阶段二交付物

```
□ 来料验证数据表（IDD、波形、幅度一致性）
□ 配对结果记录（4 颗芯片编号 + 分组）
□ 备用芯片清单
```

---

## 阶段三：I/V + 完整模拟级验证（Gate-2 + Gate-4）

### 所需设备与材料

```
□ 面包板或万能板（模拟部分建议万能板，面包板寄生电容大）
□ 配对的 4 颗 TDA1543（阶段二产出）
□ OPA1656 SOIC-8 ×2 + SOIC-8 转 DIP 转接板 ×2
□ LT3042 评估板或手搭 +9V LDO 电路
□ LT1763 或等效 +7V LDO
□ 3S LiFePO4 电池（或稳压电源模拟：10.95V / 9.0V / 7.5V 三档）
□ 精密电阻：Rf = 2.5kΩ / 2.2kΩ / 2.0kΩ 各 2 颗（0.1% 薄膜）
□ Rf_dummy = 2.5kΩ ×4
□ 偏置分压电阻：10kΩ ×12（信号 I/V ×4 + dummy ×8）
□ 偏置旁路电容：47μF 电解、2.2μF PP 薄膜、100nF C0G（按设计文档数量）
□ LPF 元件（四阶 Bessel，按仿真值）
□ 隔直电容：10μF PP 薄膜 ×2
□ 输出：47Ω + 100pF ×2
□ 示波器 + 万用表 + 音频分析仪（或声卡 + REW 软件）
□ 热电偶（或红外测温枪）
□ XMOS 开发板（提供 I2S 信号源）
```

### S3.1 I/V 直流工作点验证

**先不接信号，只确认 DC 偏置正确。**

```
接线：
  OPA1656 V+ ← +9V_IV（LT3042 输出）
  OPA1656 V- ← GND
  Rf = 2.5kΩ（反馈电阻）
  同相输入 ← 偏置点（10kΩ/10kΩ 分压自 +9V → 4.5V）
  偏置旁路：47μF // 2.2μF PP // 100nF C0G

□ 通电（不接 TDA1543 输出）
□ 测量 OPA1656 输出端直流电压

  | 声道 | 偏置输入 (V) | OPA1656 输出 (V) | 判定 |
  |------|-------------|-----------------|------|
  | L    |             |                 | □ 4.5V ± 0.5V |
  | R    |             |                 | □ 4.5V ± 0.5V |

□ 测量 +9V_IV 实际电压：______ V（应为 9.0V ± 0.2V）
□ 如果输出严重偏离 4.5V → 检查分压电阻值、OPA1656 焊接、供电
```

### S3.2 TDA1543 + I/V 完整连接

```
□ 按设计文档 §3.4 拓扑接线：
  — 4 颗 TDA1543 的 BCK/WS/DATA 并联到 XMOS I2S 输出
  — VDD = +7V（LT1763 输出，每颗 100nF + 共享 10μF 去耦）
  — 左声道组（#1 + #2）IOUTL 并联 → OPA1656(L) 反相输入
  — 右声道组（#3 + #4）IOUTR 并联 → OPA1656(R) 反相输入
  — 每颗未用通道 → 独立 Rf_dummy + 独立偏置点

□ 通电后首先测量：
  — TDA1543 每颗 VDD 引脚实际电压：______ V（应为 ~7.0V）
  — TDA1543 每颗 IDD：______ mA（用稳压电源限流读数，应 ~50-55mA）
  — OPA1656 输出 DC：______ V（应仍在 4.5V ± 0.5V）
```

### S3.3 信号通路验证

```
□ XMOS 播放 1kHz 0dBFS 正弦波
□ 示波器看 OPA1656 输出端：应为干净正弦波

  | 项目 | 预期 | 实测 | 判定 |
  |------|------|------|------|
  | L 输出 Vrms | ~1.94V | ____V | □ ±15% 以内 |
  | R 输出 Vrms | ~1.94V | ____V | □ ±15% 以内 |
  | 波形 | 干净正弦 | | □ 无明显畸变 |
  | L/R 幅度差 | < 5% | ____% | □ |

□ I/V 稳定性（10kHz 方波）：
  — XMOS 播放 10kHz 方波信号
  — 示波器看输出：无振铃、无振荡
  — □ 通过 / □ 需调整（记录现象：____________）

□ 并联对照（可选但推荐）：
  — 暂时断开每组中的一颗 TDA1543（只留一颗工作）
  — 测量单颗输出 Vrms：____V
  — 恢复两颗并联，测量 Vrms：____V
  — 预期：并联 ≈ 2× 单颗（±5%）
  — □ 通过 / □ 异常
```

### S3.4 Gate-2 验证：I/V 偏置点 + Rf 冻结 ★

**需要音频分析仪或声卡 + REW 进行 THD 测量。**

#### S3.4.1 THD 基线数据采集

```
测量条件：VDD=+7V，偏置 4.5V，+9V 供电，Rf=2.5kΩ（默认）

□ THD @ 1kHz, 0 dBFS:    ______ dB
□ THD @ 1kHz, -6 dBFS:   ______ dB
□ THD @ 1kHz, -20 dBFS:  ______ dB
□ THD @ 1kHz, -60 dBFS:  ______ dB

□ 并联 idle tone 测试：
  — -60dBFS 信号下，频谱中是否有异常杂散/闲置音
  — 记录频谱截图
  — 对比单颗 vs 并联（如执行了 S3.3 并联对照）
```

#### S3.4.2 Rf 三档对照

```
□ 更换 Rf=2.2kΩ，重复上述 4 档 THD：
  THD @ 0 dBFS: ______   @ -6: ______   @ -20: ______   @ -60: ______

□ 更换 Rf=2.0kΩ，重复：
  THD @ 0 dBFS: ______   @ -6: ______   @ -20: ______   @ -60: ______
```

#### S3.4.3 TDA1543 满幅电流实测

```
□ 断开 I/V 运放，在 IOUTL 引脚上临时串入精密电阻（如 1kΩ），
  通过测量电阻两端电压反推 IFS
□ 或者：用已知 Rf 的 I/V 输出 Vrms 反推
  IFS = Vrms_peak / Rf = (Vrms × √2) / Rf

  IFS 实测：±______ mA（预期 ~±0.55mA/颗，并联 ~±1.1mA）
```

#### S3.4.4 Gate-2 判定

```
路径 A（预期）：4.5V + Rf=2.5kΩ，THD 正常
  → 冻结 Rf=2.5kΩ，输出 1.94Vrms
  → Gate-2 通过 ✓

路径 B：Rf=2.2kΩ THD 更优
  → 降档为 Rf=2.2kΩ，输出 1.71Vrms
  → 更新设计文档输出电平

路径 C：偏置 4.0V 更优
  → 改分压为 12.7kΩ / 10kΩ，输出仍 1.94Vrms
  → 更新设计文档偏置章节

路径 D：+9V 供电不理想
  → OPA1656 V+ 跳线切到 +7V_DAC
  → 偏置改 4.3V，Rf=2.2kΩ
  → 输出降为 1.71Vrms
  → 更新设计文档电源架构

路径 E：全部异常
  → 更换芯片重测（排除个体问题）
  → 如果更换后仍异常 → 全面排查：供电噪声、布线寄生、运放振荡
```

### S3.5 LPF + 输出级验证

```
□ 接上四阶 Bessel LPF + 10μF PP 隔直 + 47Ω/100pF 输出网络
□ 用 1m RCA 线连接到后级（耳放/声卡输入）

□ 频响检查：
  — 1kHz 参考电平：______ Vrms
  — 20kHz 相对 1kHz：______ dB（应 < 1dB 衰减）
  — 40kHz 相对 1kHz：______ dB（LPF 开始衰减）

□ 镜像抑制（fs=44.1kHz 时）：
  — 44.1kHz 镜像频点衰减：______ dB（验收标准 ≥ 24dB）

□ 隔直后 DC 偏移：
  — RCA 输出端直流电压：______ mV（应 < 5mV）
```

### S3.6 Gate-4 验证：热设计 ★

**需要模拟最坏工况：电池满充电压 + 密封环境 + 连续运行。**

```
测试条件：
  — 供电电压设为 10.95V（或满充电池）
  — 模拟 v3.4.2.2 钢顶壳+铝底板的不对称散热结构：
    · 顶部/侧面用泡沫或纸板围挡（模拟 SPCC 钢的低导热率隔热屏障）
    · 底部放置一块 ~100×70mm 铝板（1.0mm 厚），用导热垫贴合 PCB 底面
      （模拟真实铝底板作为唯一散热主通道的热路径）
    · 铝板下方保留自然对流空间（不贴桌面）
  — 连续运行 2 小时，期间播放音乐

□ 每 30 分钟记录一次温度：

  | 时间 | LT1763 #2 (+7V) | LT1763 (3.3V) | TDA1543 最热一颗 | OPA1656 附近 | 环境温度 |
  |------|----------------|---------------|-----------------|-------------|---------|
  | 0min |     °C         |     °C        |     °C          |     °C      |   °C    |
  | 30min|                |               |                 |             |         |
  | 60min|                |               |                 |             |         |
  | 90min|                |               |                 |             |         |
  | 120min|               |               |                 |             |         |

验收标准（对照设计文档 §16 Gate-4）：
  □ LT1763 #2 (+7V_DAC, ~0.82W) 壳温 < 100°C
  □ LT1763 (3.3V_D, ~0.92W) 壳温 < 100°C
  □ TDA1543 最热一颗壳温 < 75°C
  □ OPA1656 附近温升 < 5°C（相对环境）
  □ 铝底板外表面温度 < 45°C（舒适触摸上限）
  □ 两小时内无热保护关断

Gate-4 判定：
  □ 全部达标 → Gate-4 通过 ✓
  □ LT1763(3.3V) 过热 → 启用预降压方案 D1（TPS62160）
  □ LT1763(+7V) 过热 → 加大铜皮面积方案，更新 PCB 布局约束
  □ TDA1543 过热 → 加大 GND 引脚铜皮 / 增加散热过孔
  □ 系统级过热 → 评估铝底板增大散热面积（脚垫加高增大底部对流空间）
```

### 阶段三交付物

```
□ Gate-2 数据表（THD 四档 × 三个 Rf + IFS 实测 + 最终冻结值）
□ Gate-4 温度记录表
□ 信号通路验证数据（Vrms、波形截图）
□ LPF 频响 + 镜像抑制数据
□ 如有设计变更 → 更新后的设计文档（Rf 值、偏置点、供电方案）
```

---

## 阶段四准入：设计冻结检查清单

**Gate-1 + Gate-2 + Gate-3 + Gate-4 全部通过后，执行以下检查清单，确认原理图可以冻结。**

### 电气参数冻结确认

```
□ 帧格式：□ 32fs（Gate-1 通过）/ □ 64fs（降级）
□ 最高采样率：□ 192kHz / □ 96kHz（降级）
□ Rf 最终值：______ kΩ（Gate-2 冻结）
□ 偏置电压：______ V（Gate-2 冻结）
□ OPA1656 V+ 供电：□ +9V_IV（默认）/ □ +7V_DAC（降级）
□ 输出电平：______ Vrms
□ 热方案：□ 标准（Gate-4 通过）/ □ 需要预降压 D1 / □ 需要加大铜皮
□ 壳体：钢顶壳(SPCC 0.8mm) + 钢前后面板(1.0mm) + 铝底板(5052 1.0mm)，全部镀镍
```

### 原理图核对

```
□ TDA1543 ×4 连接完整（BCK/WS/DATA 广播、IOUT 并联、dummy 独立偏置）
□ OPA1656 ×2 供电和偏置正确
□ LT3042 ×2 + LT1763 ×2 四路 LDO 完整
□ 晶振 ×2 + OE 控制 GPIO + 10kΩ 下拉
□ USB 保护（USBLC6-2 + CMC）
□ 去耦电容全部标注（~54 颗）
□ LPF 元件值与仿真一致
□ 隔直电容 10μF PP
□ 输出 47Ω + 100pF
□ DP3T 拨杆 + 电池管理电路
□ PCB 预留位：
  — Rf 三档（0805 焊盘）
  — OPA1656 V+ 跳线位（+9V_IV / +7V_DAC）
  — 偏置上臂双 0603 串联焊盘
  — BCK 串联电阻预留（0Ω/22Ω/33Ω）
  — Rs 预留（0Ω/10Ω/33Ω）
```

### PCB 布局约束确认

```
□ 晶振到 XMOS MCLK 输入 < 15mm，同层，最少过孔
□ I2S 走线（BCK/WS/DATA）等长或差异可忽略
□ TDA1543 IOUT 并联走线：星形汇合到 I/V 虚地节点
□ USB 走线到模拟走线 ≥ 10mm
□ TDA1543 GND 独立过孔
□ LT1763 ×2 布局互相远离（热场不叠加）
□ Gate-4 如有额外散热需求 → 标注在布局约束中
□ 整板目标尺寸 ~90×60mm（确认可装入 100×70×30mm 钢顶壳+铝底板壳体）
```

### 文档同步确认

```
□ roger_dac_v3.4.2.md 中所有 Gate 记录已填写（日期 + 结果 + 备注）
□ 如有降级或变更 → 相关章节已更新（BCK 频率表、采样率支持、输出电平等）
□ BOM 与冻结后的参数一致
□ 所有测量数据已存档
```

### 最终签核

```
□ 以上全部打勾 → 原理图冻结，可以发板厂
□ 如有未完成项 → 标注并评估是否阻塞出板
   未完成项：__________________________________________________
   是否阻塞：□ 是（必须完成）/ □ 否（可以带风险出板，PCB 上有预留）
```

---

## 附录 A：快速参考 — 测量接线速查

### I2S 信号测量点（XMOS 开发板）

```
查阅 XK-AUDIO-216-MC 开发板原理图，确认以下引脚的 header 位置：
  BCK  → Port 1D (具体 pin 见开发板文档)
  WS   → Port 1C
  DATA → Port 1A
  MCLK → Port 1E (外部 MCLK 输入)

注意：开发板的 I2S 引脚可能通过 header 暴露，也可能需要飞线。
阶段一开始前务必确认物理测量点。
```

### 简易偏置电路（阶段二功能测试用临时方案）

```
+9V (或 +7V 如果没有 +9V LDO)
 │
 10kΩ
 │
 ├──── 100nF ──── GND
 │
 ├──── Rf (2.5kΩ) ──── TDA1543 IOUTL/R
 │
 10kΩ
 │
 GND

偏置点 ≈ Vsupply / 2
这是临时方案，仅用于阶段二看波形。阶段三用正式的 OPA1656 I/V。
```

## 附录 B：风险与降级路径速查

| 风险事件 | 影响 | 降级路径 | 设计文档参考 |
|----------|------|----------|-------------|
| XUA_I2S_N_BITS 不生效 | 无法输出 32fs | 修改 lib_xua 源码 → 或全面 64fs + 96kHz 上限 | §3.6, §16 Gate-1 |
| TDA1543 整批假货 | 无 DAC 芯片 | 换供应商 → 或退回 AD1865/推进 v3.0 | §3.8 |
| +9V 供电 THD 异常 | I/V 设计变更 | 跳线切 +7V，偏置改 4.3V，Rf=2.2kΩ | §5.4, §16 Gate-2 路径 D |
| LT1763 热关断 | 电源不稳 | 加预降压 TPS62160 | §16 Gate-4 |
| OE 切换毛刺 | 时钟不干净 | 加大延迟 → 或加 MUX 缓冲 | §2.2, §16 Gate-3 |

---

*文档版本: 1.1*
*对应设计文档: roger_dac_v3.4.2.2*
*创建日期: 2026-02*
